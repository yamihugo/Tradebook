# PROJECT-SPEC.md — A Bíblia do Projeto

> **Estatuto:** este documento é a **bíblia do projeto** — o mapa autoritário. Qualquer
> chat novo (**Cody, Max, Nathan**, ou um agente qualquer) carrega-o no arranque e
> **não inventa nem contradiz** o que aqui está.
>
> **Bíblia, não lei.** As *regras* vivem nos docs normativos (`UX-GUIDELINES.md`,
> `STRATEGY-DATA-CONTRACT.md`, `COPY-TRADING.md`, `BACKUP-AND-EXPORT.md`,
> `RELEASE-AND-DISTRIBUTION.md`, `PAYOUTS.md`) e no `AGENTS.md`. Esta bíblia **liga**
> para eles e dá o essencial. O **catálogo** de UI (tokens/cards/states) vive em
> `UI-CATALOG.md`; a **estrutura do código** em `ARCHITECTURE.md`.
>
> **Estado:** revisto 2026-09-18. Canónico em `source/docs/`; `TradeBook/docs/` é symlink.

---

## 0. A linha que nunca cruzamos (ler primeiro)

**Isto é um journal, não uma prop firm. O journal reporta; a prop firm impõe.**
(product rule, 16 Sep 2026 — permanent.)

- Mostramos a regra, o espaço que resta, o uso e a direção. **Nunca** impomos: sem
  bloquear ordens, sem recusar trades, sem reter payouts, sem limites de trades, sem
  lockouts, sem bloqueio de símbolos.
- **Os números ficam honestos mesmo quando são maus** — um limite rebentado lê-se como rebentado.
- **Onde o nosso modelo é só um modelo** (pernas de copy, comissões simuladas) **dizemo-lo**:
  a plataforma é a fonte da verdade; nós somos o registo dela.
- O que só faz sentido para o back-office de uma firma **não pertence aqui**.

Declaração completa com fontes: `UX-GUIDELINES.md` §0. Cada ecrã novo é julgado pela
checklist no fim desse ficheiro.

---

## 1. Identidade & Arquitetura

### 1.1 Identidade

- **Plugin id:** `tradebook` — **permanente, nunca muda** (ver `RELEASE-AND-DISTRIBUTION.md`).
- **Nome visível:** Tradebook · **Autor:** YamiHugo · **Versão de manifesto:** 0.4.9 (dev 0.5).
- **Domínio:** journal de trading para futuros Tradovate (NQ/ES/MNQ/MES).
- **Princípio base:** 100% local — os dados são ficheiros markdown na vault, sem cloud nem subscrição.
- **Relógio do journal:** o journal mostra e guarda sempre o **wall-clock da zona do journal**
  (`settings.timeZone`, por omissão **`America/New_York` / ET** — a convenção de mercado dos
  futuros). Na importação, o CSV do broker é convertido da **zona de origem**
  (`settings.importZone`, ou auto-detectada / perguntada uma vez por broker) para a zona do
  journal; cada trade guarda o seu `timezone` congelado. As horas manuais aparecem etiquetadas
  com a zona (`ET`, `CT`, …). Nada é imposto: tudo é editável inline.

### 1.2 Entry point & registo

- **Classe:** `TradebookPlugin` em `src/main.ts`.
- **Vistas** (`main.ts`): Home `tradebook-home-view` · Dashboard `tradebook-dashboard-view` ·
  Strategies `tradebook-setups-view` · Print Queue `tradebook-print-queue` (right sidebar) ·
  Account dashboard `tradebook-account-dash-view` · Add Trade `tradebook-add-trade` ·
  Trade Log `tradebook-trade-log-view` · Accounts `tradebook-accounts-list-view` ·
  Trade Detail `tradebook-trade-detail-view` · Sidebar `tradebook-sidebar-view`.
- **Ribbon:** `grip` → Home · `wallet` → Accounts · `list` → Trade Log · `plus` → Add Trade.
- **Camadas, lib, data attributes:** ver `ARCHITECTURE.md`.

---

## 2. Design & Convenções

Tudo o que é catálogo (tokens `--tj-fs-*`/`--tj-fg-*`/`--tj-sp-*`, temas, cards por
view, states, tooltips, gráficos, convenções CSS/JS) vive em **`UI-CATALOG.md`**.

Regras que **não** se negociam (detalhe em `UX-GUIDELINES.md`):
- Escala de tipo única; contraste WCAG AA; **nunca** cor sozinha para transmitir estado.
- Alvos de toque ≥ 24×24; reordenar oferece setas além de drag.
- `styles.css` é prettier multi-line; prefixo `tj-`; **sem código nem CSS morto**.

---

## 3. Copy (textos) — princípios

- **UI em inglês.** Respostas dos agentes em **PT-PT**. *(Sem i18n.)*
- **Tom:** direto, calmo, premium; sem gamificação nem exclamações a mais.
- **Honestidade:** números maus leem-se como maus; modelo-only diz "model".
- **Nunca impor:** verbo de report, não de enforcement (§0).
- **Todo o texto de UI está inline nos views** (não há ficheiro de strings). Ao adicionar
  copy, seguir o tom acima; se for título reutilizável, centralizar na tabela certa
  (`CARD_TITLES`, `METRIC_TITLES`, `BAR_CATALOG`/`MINI_CATALOG`, `DEFAULT_LABELS`).
- **Navegação (labels fixos):** Home · Trade Log · Strategies · Accounts · Add Trade.
  Secções: Overview · Tools. Busca: placeholder "Search anything — date, symbol, P&L, strategy…".

---

## 4. Contratos de dados

- **Estratégias:** `STRATEGY-DATA-CONTRACT.md` — Setup = string simples; a verdade são as
  notas; comparação case-insensitive + trim; rename first-class; campos novos são aditivos.
- **Copy trading:** `COPY-TRADING.md` — entidade/campo, consumidores, "no wrong numbers"
  (conta = pernas próprias, dedupe por `copyBaseKey`, lifecycle, pernas congeladas).
- **Contas:** os templates de prop firm são **semente**; as regras são **copiadas para os
  dados do utilizador** — mudanças de firm nunca tocam em contas existentes.
- **Backup/export:** `BACKUP-AND-EXPORT.md` — Caso A: um ficheiro JSON re-importável
  `tradebook-backup-<YYYY-MM-DD>.json` em `backups/`. Caso B (CSV): depois do lançamento.
- **Payouts:** `PAYOUTS.md` — o journal não é prop firm; métricas de performance nunca
  incluem cash-flows.
- **Futuros/custos:** `futures.ts` é a fonte (NQ $20/pt, ES $50/pt, MNQ $2/pt, MES $5/pt).

---

## 5. Build · Test · Deploy

> Comandos completos e harness: **`AGENTS.md`** (não duplicar). Resumo:
> `npm run build` → 0; smoke em `~/trading-journal-smoke` → **142 PASS / 0 FAIL**;
> `node tools/ux-audit.mjs` → sem violações; deploy para
> `<vault>/.obsidian/plugins/tradebook/` (nunca `data.json`, verificar md5, pedir Ctrl+R).
> Artefactos em `/home/hugo/tj-out/`.

---

## 6. Mapa dos documentos

| Documento | É… | Carregado sempre? |
|---|---|---|
| `AGENTS.md` | regras de trabalho do repo; a lei | ✅ |
| `PROJECT-SPEC.md` | **esta bíblia**; o mapa | ✅ |
| `DEV-REFERENCE.md` | plataforma (Obsidian), BRAT, linguagens e links | ✅ |
| `UX-GUIDELINES.md` | regras de UX/design + §0 | ✅ |
| `STRATEGY-DATA-CONTRACT.md` | contrato de dados de estratégias | ✅ |
| `COPY-TRADING.md` | sistema de copy trading | ✅ |
| `RELEASE-AND-DISTRIBUTION.md` | versionamento/BRAT | ✅ |
| `PAYOUTS.md` | payouts + regra de cash-flows | ✅ |
| `UI-CATALOG.md` | tokens, cards, states, CSS convenções | referência |
| `ARCHITECTURE.md` | camadas + lib + data attributes | referência |
| `BACKUP-AND-EXPORT.md` | decisão de backup/export | referência |
| `VAULT-STRUCTURE.md` | modelo de pastas + regra "descobrir pelo `type`" | referência |
| `STRATEGIES-2.0.md` | visão/plano das estratégias 2.0 | referência |
| `ROADMAP.md` / `BACKLOG-AND-HISTORY.md` | ideias e estado (feito/por fazer) | referência |
| `QA-CHECKLIST.md` | registo vivo de QA (manter atual) | referência |

**Canónico vs espelho:** o canónico é `source/docs/`. `TradeBook/docs/` é um **symlink**
para o canónico — editar num sítio é editar no outro.

---

## 7. Registo de alterações

| Data | Alteração |
|---|---|
| 2026-09-17 | Criação da bíblia (v1): inventário completo de views, cards, tokens, states, copy, comportamentos e convenções. |
| 2026-09-17 | `TradeBook/docs` passou a symlink para o canónico; criado `DEV-REFERENCE.md`. |
| 2026-09-18 | **Renomeado para Tradebook**: plugin `id` → `tradebook`, classe `TradebookPlugin`, view types `tradebook-*`, deploy `.obsidian/plugins/tradebook/`, backup `tradebook-backup-<data>.json`, pasta por omissão `Tradebook/trades`. |
| 2026-09-18 | **Modelo de timezone**: relógio único (`settings.timeZone`, default ET); import converte da **zona de origem** para ET; cada trade guarda `timezone`; horas manuais etiquetadas com a zona; entry/exit time e preços editáveis inline. |
| 2026-09-18 | **Limpeza do repositório**: docs pessoais removidos; catálogo de UI movido para `UI-CATALOG.md` e arquitetura para `ARCHITECTURE.md`; a bíblia ficou só com lei + mapa (menos tokens por sessão). |
| 2026-09-18 | **Conformidade com as regras oficiais do Obsidian**: auditoria às developer docs; workflow CI com *build provenance attestation* e release em *draft*; `innerHTML`/`outerHTML` eliminados do `src/`; regras de plataforma (deferred views, lifecycle, sentence case) em `DEV-REFERENCE.md` §1.2; submissão via `community.obsidian.md` + ESLint oficial em `RELEASE-AND-DISTRIBUTION.md` §8–9. |
| 2026-09-18 | **Logos das firms embutidos no bundle**: como Obsidian/BRAT só descarregam `main.js`/`manifest.json`/`styles.css`, os 4 PNGs passam a ser *data URIs* (esbuild `loader: dataurl` + `src/globals.d.ts`); `firmLogoUrl()` devolve o data URI. PNGs otimizados (128px, 220KB→28KB). `README.md` ganha tutorial de instalação via BRAT + nota de segurança de dados; criado `KNOWN-LIMITATIONS.md`. |
| 2026-09-18 | **Custos persistem (A)**: `commission` e `fees` passam a ser escritos no frontmatter da nota e lidos no parser (`storage.ts`); antes viviam só nas pernas de fill e desapareciam no primeiro reload (Fees $0,00). Sem migração — re-importar. |
| 2026-09-18 | **Custos órfãos (B)**: linhas de custo que a Cash History cobra por execução mas o Orders agrega numa ordem passam a **colar-se** ao trade quando é óbvio (mesmo contrato + timestamp dentro de entry→exit ±2 min); o que sobra vira um **custo datado da conta** (`FeeAdjustment.kind:"cost"`, oculto do modal *Correct fees* mas no saldo) e o review de import mostra *platform · glued · in account* + comparação ✓/⚠ com o `Amount` final da Cash History. `ImportCosts` perdeu os campos mortos `matched`/`unmatched`/`paired`/`deposits`. |
| 2026-09-18 | **Delete duro (C)**: apagar uma conta passa a remover também registos (payouts, depósitos, correções/custos), mapeamentos, laços de copy **e as notas de trade para o lixo do Obsidian** (`vault.trash`), com dupla confirmação que lista exatamente o que morre. |
| 2026-09-18 | **Arquivo fora de tudo (D)**: contas arquivadas saem das métricas de performance, do saldo da carteira e dos totais (Home e portefólio); os trades mantêm-se acessíveis no Trade Log e Strategies. `activeAccounts()`/`isArchivedTrade()` centralizam a regra. |
| 2026-09-18 | **Ordem do header da conta (E)**: payout (carteira) · Correct fees (recibo) · Account settings (roda) — o gear fica no canto direito. |
| 2026-09-18 | **Guard de conta no import (F)**: o CTA `Import N trades` está desativado enquanto não houver ≥1 alvo (um nome da CSV mapeado para conta **ou** uma conta marcada em *also record these trades in*), com mensagem inline "Select at least one target account to proceed."; trades sem conta **não são escritos** e o review diz quantos ficaram de fora. |
| 2026-09-18 | **Import honesto (G)**: o recibo deixa de reportar o pedido (`trades.length`) e passa a provar o gravado — depois de `storeTrades`, relê a vault e confirma cada nota esperada; se 0 notas ou alguma não confirmada, mostra `Retry` e uma `Notice`, nunca "Done". Corrige o falso-sucesso intermitente. |
| 2026-09-18 | **Foco do saldo no Correct fees (H)**: o campo "What I have" foca ao abrir e ao clicar em qualquer ponto da linha; `:focus` passa a `--interactive-accent` (hover mantém `--text-muted`). |
| 2026-09-18 | **Janela automática do Correct fees (I)**: início = dia a seguir ao `period.to` da última correção (ou `createdAt`); fim = último dia com trade da conta (editável). Os trades são casados por **id** (`mappedAccount`), não por nome. |
| 2026-09-18 | **Motor de fees proporcional (J)**: `allocateEqually` → `allocateProportional` (por nº de contratos, cêntimos inteiros, maior-resto, soma exata); o Post-Trade Review mostra "Fees corrected" (etiqueta **model**) ao lado das Fees reais da plataforma, via `feeForTrade`/`allocatedKeysFor` — nenhuma nota é reescrita. |
| 2026-09-18 | **Follow-up do Correct fees (K)**: foco do saldo fiável (handler no `.tj-mg-row` inteiro em `mousedown` + reposição após cada render, fundo accent no `:focus`); helper sob "Spread over"; **chave de fatia endurecida** (`tradeFeeKeys` = `fillId` → caminho da nota → composta antiga como fallback, casamento por qualquer variante) com **aviso de fatias órfãs** no modal; a linha "Fees" do Post-Trade Review passa a mostrar `$total · $X corrected` com tooltip *model* (linha separada fundida). |
| 2026-09-18 | **Contas dirigidas pelo utilizador (L)**: o wizard passa a ter 4 passos (Type · Identity · Rules · Review) e as regras são escritas pelo trader — target/max loss ($ ou %), daily loss, tipo de drawdown, posição e dias mínimos; o preset de firm é apenas um ponto de partida opcional (Journalit-style), nunca a espinha. Passo 2 junta nome, saldo inicial (um só campo, que é o `size`) e uma grelha de logos (prop firms + brokers + Own em símbolo CSS + iniciais "Custom"); `PropAccount.branding` guarda `packaged`/`initials`. Novo resolvedor `lib/accountRules.ts` (`resolveAccountView`) serve Home, cartões, conta e Settings — o antigo hard-fail "Unknown firm/program" desapareceu. `lib/firmLogos.ts` centraliza o catálogo e os data URIs (faltam 8 PNGs por colar). "Quick add" saiu das Settings: o wizard é a única porta. Management: banners → tooltips `(i)`, empty state de copy groups com diagrama de nós, badges LEADER/COPIER (adeus 👑), tipos em pills. |
| 2026-09-18 | **Polimento do wizard (M)**: passo 1 com ícones Lucide (adeus emojis) e cartão ativo com borda `--interactive-accent` + glow discreto; passo 2 com o **nome ligado ao saldo/firm** (auto `Topstep · $100K`, editável; `nameTouched` liberta a ligação e "Create & add another" volta a ligá-la), grelha de marcas em 3 secções (Prop firms · Brokers · Practice) com pills de altura fixa e iniciais "Custom"; passo 3 em grelha de 2 colunas com `$`/`%` como afixos estáticos dentro do campo e um toggle `$ | %` inline (adeus `<select>`); passo 4 com tabela chave-valor (rótulos muted, valores à direita a 700) e stepper de lote proeminente. `firmLogos` ganha o grupo `practice` (id `own` mantém-se). |
| 2026-09-18 | **Wizard, passo 2/3 (N)**: passo 2 com **Nome + Saldo numa linha de 2 colunas** (`.tj-wz-row-2`, empilha <560px) para o modal não crescer com a grelha de marcas. Passo 3: dropdown de drawdown endurecido dentro do modal (`.tj-account-wizard .tj-dd-*` com `!important`, porque o Obsidian pinta os botões por cima — parecia um `<select>` nativo); toggle `$ | %` com track (`.tj-wz-untoggle`) e contraste subido. Presets de firm substituídos por **5 tamanhos genéricos Standard** (25/50/100/150/300K) que preenchem só as regras — target/max loss/daily loss a 6%/4%/2% — **nunca o saldo**; o aviso de que as regras das firms mudam fica sempre visível. |
| 2026-09-18 | **Import, conta de destino (O)**: o importador deixa de escrever para o vazio. `guessMapping()` resolve cada nome da CSV por ordem de certeza — conta que já responde ao nome → **a única conta do journal** (depois de um reset não há o que adivinhar) → `settings.lastImportAccountId` → `settings.lastCreatedAccountId` → "Leave unassigned". Um **único tick** em *Also record these trades in* passa a servir de conta-base quando não há mapping (`tickBaseId()`) e é retirado do broadcast de pernas (sem trade escrita duas vezes). O wizard grava a conta criada em `lastCreatedAccountId`; o dropdown do importador grava `lastImportAccountId`. **Tipo do ficheiro lido pelo header** (`csvKind()`: `cash` · `orders` · `fills` · `unknown`), com mensagem explícita para um ficheiro que não é Orders/Fills em vez de um silêncio. |
| 2026-09-18 | **Wizard, passos 2/3 (P)**: o passo 2 passa a ser **só marca** (grelha Prop firms · Brokers · Practice · Custom, sem nome nem saldo) e o passo 3 passa a "Account" — **preset no topo** (`Standard · $50K`), depois **Nome + Saldo inicial** na linha de 2 colunas, depois as regras. Escolher um preset preenche o **saldo** (= `size`), o **nome** (auto `Tradeify · $50K`, via `baseName()` enquanto `nameTouched` for falso) e target/max loss/daily loss (6%/4%/2%). Passos renomeados para Type · Brand · Account · Review e `flow()` = `[0,1,2,3]` para todos — personal/demo fazem o passo 3 só com a identidade. Isto **inverte a decisão anterior** "o preset nunca toca no saldo". |
| 2026-09-19 | **Wizard, passo 3 (Q)**: sai o campo manual *Initial balance* — o tamanho escolhe-se só pelo dropdown (`$25K · $50K · $100K · $150K · $300K · Custom…`, sem a palavra "Standard"), com **Custom…** a revelar um campo `$` cujo valor actualiza o tamanho **e** o nome da conta (`Tradeify · $37K`). As regras continuam a encher-se a 6%/4%/2%. Novo campo **Started on** (obrigatório): sem tamanho e sem data o `Next` fica desactivado, com o helper "Choose a size and a start date to continue." — é esta data que impede o filtro `t.date < acc.createdAt` de esconder histórico importado. |
| 2026-09-19 | **Wizard, passo 3 (R)**: o **Account size** passa a nascer **vazio** e é obrigatório — já não vem `$50K` por omissão, para que o trader declare sempre o tamanho. O `Started on` nasce também **vazio** (não a data de hoje). O `Next` só desbloqueia com **tamanho > 0 E data** escolhidos, e o helper diz só o que falta: "Choose a size and a start date to continue." · "Choose a size to continue." · "Choose a start date to continue.". O nome mostra só a marca enquanto não há tamanho (`Tradeify`) e passa a `Tradeify · $50K` quando ele é escolhido (via `baseName()`, enquanto `nameTouched` for falso); as regras continuam a encher-se a 6%/4%/2% no momento em que o tamanho é escolhido ou o tipo muda com tamanho já definido. "Create & add another" volta a limpar tamanho, data e regras. |
| 2026-09-19 | **Nome com tipo + calendário próprio (S)**: o nome da conta passa a `marca tipo $tamanho` (`Tradeify Eval $50K`, `Tradovate Personal $50K`), usando as labels configuráveis (`typeLabel`), e continua ligado ao tamanho/marca até o trader escrever o seu. O calendário nativo do sistema (`input[type=date]` + `showPicker`) sai: `mountDateField` abre agora o **calendário da casa** (`lib/calendar.ts` — grelha do mês à segunda, "hoje" com contorno, dia escolhido com acento `--interactive-accent`, "Today", setas/Enter/PageUp-Down, popup `fixed` preso ao campo). A API mantém-se, por isso os 23 sítios com datas ganham o mesmo calendário; escrever a data à mão continua a funcionar. |
| 2026-09-19 | **Management: presets de cartão, cor de grupo e tipos (T)**: o separador **Cards** deixa de ter 9 dropdowns por tipo e passa a **3 presets nomeados** por tipo — *Firm layout* (o default assinado de cada tipo, pré-seleccionado), *Results* e *Consistency* — guardados em `accountCardPreset: Record<tipo,id>` (`lib/cardSlots.ts` ganha `CARD_PRESETS`/`presetFor`/`CardLayout`; `accountCardBars`/`accountCardMini` saem, sem migração). A **cor do grupo de copy** passa a ter consequência: `copyGroupColor` tinge a secção do grupo e as tags Leader/Copier na página de Contas (`groupTint()`). O "Account types shown" sai do **Display** e passa para **Types** como pills de olho (`.tj-mg-vis`, `eye`/`eye-off`); o Display fica só com Layout + What appears. A relação líder→copiers ganha uma **árvore** (`.tj-mg-tree`, conectores CSS) e o "Remove" deixa o `title` nativo e passa a `attachTip`. O contraste do bloco Management sai do token do tema `--text-muted` para `--tj-fg-2`/`--tj-fg-3`. |
| 2026-09-19 | **Cards só por preset + empty state dos copy groups (U)**: o separador **Cards** do Management **sai** — deixou de haver edição por utilizador. O cartão de cada tipo de conta passa a ter um **layout fixo assinado** em `lib/cardSlots.ts` (`layoutFor(type)` → `{bars, mini}`; `CARD_PRESETS`/`presetFor`/`accountCardPreset` removidos, sem migração): eval `target·drawdown` / `toTarget·win·profitFactor·last`; funded `dailyRoom·drawdown` / `trades·win·paid out·last`; live `dailyRoom·drawdown` / `trades·win·**day win**·avg R` (novo mini `dayWin`, `m.dayWinRate`); personal/demo/unknown `greenDays·ddFromPeak` com as quatro métricas de sempre. O Management fica com **3 separadores** (Copy groups · Display · Types) e o `previewFor`/`CardSlotOverride` da lista caem. O **empty state dos copy groups** perde o diagrama de nós: passa a um título calado ("No trading groups yet") + uma frase + o botão "+ Create Copy Group", e a nota `ⓘ` "Linking never rewrites trades already recorded…" só aparece **quando já há grupos**. |
| 2026-09-19 | **Management: compositor guiado e Types dentro do Display (V)**: o "New trading group" passa a ser um **compositor em três passos numerados** — **1 Leader** (grelha de cartões de conta `.tj-mg-leadcard`, o escolhido com borda `--interactive-accent` + glow; já **não** há dropdown nem líder pré-selecionado), **2 Copiers** (bloqueado com "Pick the leader first." até haver líder; depois mostra o chip `.tj-mg-lockedlead` e uma linha por conta com a caixa à esquerda e o sub "copies <líder>") e **3 Copy from** (também bloqueado até haver líder, com a nota explicativa sempre visível). O botão **Create group** só liga com líder escolhido. As notas das opções de "Copy from" passam a linguagem de trader. O separador **Types** desaparece: as suas linhas (nome, check, cor, setas, olho, Reset) passam para dentro do **Display** como terceira secção (`renderTypeRows`), ficando **2 separadores** (Copy groups · Display). |
| 2026-09-19 | **Management: portal do dropdown, estado do copy e disband (W)**: o dropdown da casa (`lib/dropdown.ts`) passa a **portal** — a lista sai para `<body>` presa ao botão com `position: fixed`, `z-index: 1100`, flip para cima quando não há espaço, reposiciona em `scroll`/`resize` e fecha em clique-fora/Escape; um `MutationObserver` fecha o menu se o anfitrião sair do DOM. Acaba o corte da lista pelo `.tj-manage-body` (o scroll do painel deixa de a apanhar) nos 19 sítios que o usam. **Compositor compactado** (`.tj-manage-body` 70vh, paddings menores, lista de copiadores a 120px) para caber sem barra. **Estado do copy**: novo `unlinkCopier()` em `lib/copy.ts` (fecha o período, limpa `copyRole`/`copyBaseId`/`copyMultiplier`/`copySizing` e afins, **guarda** `copyPeriods`/`copyConfigHistory` — é o registo do que correu) usado pelo Remove, pelo Move group e pelo novo "Take out"; o rácio passa a escrever via `startCopying` (a config datada é o que o motor lê); o pick morto do "Add to group" passou a `Notice` em vez de clique silencioso e o reset inválido `"today"` voltou a `"start"`. **Nunca há beco sem saída**: o botão `+ New trading group` fica sempre vivo e, sem contas livres, o passo 1 lista as contas ocupadas com um "Take out" de um clique. **Disband** por um quadrado de lixo (24×24) no cabeçalho do grupo, com confirmação em dois toques inline (nada de notas é tocado) e `Notice` ao criar/rebentar/ligar/desligar. |
| 2026-09-19 | **O símbolo é do motor, não do trader (X)**: sai o dropdown manual **"Same symbol / Mini ↔ micro"** do copiador — era um no-op comprovado (o `startCopying` grava sempre uma entrada em `copyConfigHistory` e o `copyCrossOrder` só era lido na ramificação legacy, por isso mudar o toggle nunca tinha efeito) e o campo `PropAccount.copyCrossOrder` desaparece. **A regra passa a ser aritmética**: cada ligação grava `crossOrder: true` + `crossMode: "exposure"` e o motor decide por trade em `buildLeg` — espelha em **micros só quando `baseQty × ratio < 1`** e o símbolo tem micro (1 mini = 10 micros; exposição preservada, NQ $20/pt = 10 × MNQ $2/pt). Fora desse caso fica o símbolo do líder (`1 NQ × 1` → `1 NQ`; `2 NQ × 0.5` → `1 NQ`); um símbolo sem micro (`M2K`) continua a arredondar para baixo. As **pernas já escritas ficam congeladas** (só as futuras mudam), os fills importados ganham sempre, o `copySymbolMap` (`NQ → MNQ`) continua a documentar a travessia na nota e a tooltip do rácio explica a travessia automática. |
| 2026-09-19 | **Sem bolha nativa, ritmo uniforme (Y)**: novo `lib/numeric.ts` (`freeNumeric`) — todos os `input[type=number]` do plugin (18 sítios em 8 ficheiros: Manage rácio, wizard afixos/amount/size, regras e multiplier/amount da conta, payout, fee-adjust, os 6 do Add Trade, a factory de linhas editáveis do Trade Detail e o editor de regras das Settings) deixam de ter `min`/`max`/`step` no markup e ganham `step="any"` + `invalid` cancelado + `novalidate` no form; os limites continuam a ser impostos nos handlers (a bolha *"Please enter a valid value"* era do Chromium e não se estiliza). Campos mortos das opts de `editableRow` (`suffix`/`min`/`max`/`step`) removidos. **Ritmo das secções do Management**: `.tj-manage-sect` passa a `--tj-sp-5`, o `.tj-manage-secthead` ganha `--tj-sp-2` até ao conteúdo (era 0 — colava o título `Types` à lista), o tracking do título desce a `.12em`, notas/empty e cartões de grupo fecham com `--tj-sp-4`; o helper morto `private section()` sai. |
| 2026-09-19 | **Tooltips: uma só, e a certa (Z)**: os `(i)` do Management mostravam sempre a mesma tooltip genérica *"More information"*. Duas causas somadas: (1) o `attachTip` **não** punha a classe `tj-tip-anchor` no elemento, e o guard (`guardTips`, `mousemove` em captura) matava a nossa tooltip no movimento seguinte — corrigido no próprio `attachTip`, o que conserta os **113** sítios de uma vez; (2) o `aria-label: "More information"` fazia o Obsidian desenhar a tooltip dele, e era a única que sobrava. O `(i)` passa a ter o glifo `aria-hidden` e um `span.tj-sr-only` com o nome acessível `About <título>`; os três textos (Layout · What appears · Types) ficam curtos e distintos. Nova classe `.tj-sr-only`. Regra permanente: **nunca `aria-label` num âncora de `attachTip`**. |
| 2026-09-19 | **Duas superfícies, um só glifo, e a faixa honesta (AA)**: o Management deixa de ter separadores — `openCopyGroups` e `openAccountsDisplay` abrem a mesma modal em dois modos (Copy groups · Display), e o header das Contas passa a ter **três quadrados** (`users` Copy groups · `sliders-horizontal` Display · `plus` Add account, este último `.is-primary`); o quadrado das copy groups fica desactivado com tip quando há menos de duas contas. O `(i)` deixa de ser a letra dentro de um aro nosso: passa a ser o pictograma de informação do Obsidian (`setIcon(el,"info")`, 14px, `--tj-fg-3`) nos 6 sítios — `.tj-info-dot` perde borda/raio/font-size, `.tj-manage-infoico` ganha o mesmo `svg`. A tooltip do gráfico da carteira deixa o `title` nativo (≈250 caracteres, proibido pela UX-GUIDELINES §5) e passa a `attachTip`; os cinco textos da faixa encurtam. **Os payouts passam a seguir a janela do gráfico** (como o Net P&L, o Growth e os Trades); só o *In accounts* fica all-time, e di-lo no sub. Catálogo de logos: **14 marcas** com PNG embutido a 128×128 (prop: Topstep · Tradeify · Apex Trader Funding · Take Profit Trader · MyFundedFutures · Lucid Trading · Alpha Futures · FTMO Futures · FundedNext · TopOne Futures; brokers: Tradovate · NinjaTrader · Interactive Brokers · AMP Futures; `own` prática em símbolo CSS); **`alphacapital` sai** — o PNG que existia era a **Alpha Futures**, não a Alpha Capital Group. |
| 2026-09-19 | **Import: a conta primeiro, e nunca adivinhada (AB)**: o importador deixa de pré-selecionar. `guessMapping()`/`resolveFallback()` saem e os settings mortos `lastImportAccountId`/`lastCreatedAccountId` desaparecem (interface, `DEFAULT_SETTINGS`, a escrita no dropdown e no wizard) — um nome da CSV que coincide com uma conta é coincidência de texto, não uma instrução, e adivinhar era como os trades caíam na conta errada com o ecrã a dizer que correu bem. O bloco de escolha (`.tj-import-pick`, "Where these trades go") passa a ser o **primeiro** da página, acima do fuso e dos custos, com estado `is-empty`/`is-set` (`N of M chosen`), a contagem e o intervalo de dias de cada nome do ficheiro (`3 trades · 19 Aug → 14 Sep 2026`) e uma hint honesta. **Só depois de haver conta** é que o *This Trading Group* e o *Also record these trades in* são desenhados; `tickBaseId()` desaparece e a regra passa a ser uma só — a conta escolhida no dropdown é onde os trades são escritos (`baseIds()`). O dropdown da casa (`lib/dropdown.ts`) ganha **cabeçalhos e chips** (`heading`, `tag`, `tagTone`): a lista lê **Leaders** · **Copiers** · **Standalone**, com chip âmbar Leader, chip acento `Copier ×N` (também no botão, depois de escolhido) e a nota *Leads N accounts* / *Copies \<líder\>*. `assets/firm-logos/`: FundedNext reduzido a 128×128 e **FTMO invertido para branco** mantendo o alfa (a marca é monocrómica e a preto desaparecia no tema escuro). |
| 2026-09-19 | **Import: o id nunca ao ecrã, e a escolha sobrevive (AC)**: a linha do *Where these trades go* mostrava o **número de conta do broker** que vem no Orders CSV (ex. `LFE0509`) — proibido desde o início. Passa a ser uma linha só de factos (`3 trades · 19 Aug → 14 Sep 2026`) quando o ficheiro traz **um** nome (o caso normal) e `Account 1` / `Account 2` (ordem do ficheiro) quando traz vários; o id fica a ser apenas a **chave interna** do `mapping`. E a escolha da conta deixou de morrer a meio: a limpeza do `mapping`/`groupSeeded` saiu do `parseTrades()` e passou para o `handleFiles()`, no momento em que um **ficheiro de trades novo** é lido — trocar o timezone, mexer no toggle dos custos, "Choose another" ou largar o Cash History re-lêem o mesmo ficheiro e **mantêm a conta escolhida e os ticks** (antes apagavam-nos, porque sem `baseIds` o review também limpava o `includeIds`); depois de cada parse só caem as chaves cujo nome já não consta de `accountsSeen`. |
| 2026-09-19 | **Página da conta, quatro limpezas (AD)**: o bloco **Copy history** sai das duas superfícies — `.tj-acc-copyhist` no `renderCopyBar` e a secção "Copy history" (`.tj-as-periods`/`.tj-as-period`) do painel Copy-trading do modal; fica só o chip Leader/Copier com o rácio, e os `copyPeriods` continuam **nos dados** (impedem que mudar de líder reescreva trades antigos) mas deixam de ser desenhados. O "rectângulo" sobre o título *Trades in this account* era o **header sticky** da tabela a flutuar sobre o título do widget (quem faz scroll é a página, não a tabela): novo `TradeTableOpts.stickyHeader?: boolean` (default `true`), a página de conta passa `false`, o `<table>` ganha `.is-static` e `.tj-tbl.is-static thead th { position: static; }` — o Trade Log mantém o sticky. O texto do fundo de todas as contas era a **regra da firm**: saem o aviso de consistência (`tj-account-note tj-account-warn`) e a prosa `size.note` (por programa, em `props.ts`), com os locais que só o aviso usava (`dayNets`/`grossProfit`/`bestDay`/`consBasis`/`consistency`/`consistencyNeed`) e o CSS `.tj-account-note`/`.tj-account-warn`; o `note` fica no catálogo como semente, já sem consumidor no ecrã. O indicador de fills deixa de ser pill de acento e, segunda passagem, deixa de ser texto: numa trade escalada é a **própria quantidade com um chevron** («6 ⌄», ou «2/4 ⌄» na parcial), sem a palavra "fills", herdando o tipo/cor do número (`.tj-tbl-fills` com `--tj-fs-body` e `color: inherit`, `min-height: 24px`, SC 2.5.8), com o chevron a acender a `--interactive-accent` no hover e com a linha aberta; tooltip única no botão, nome acessível por `<span class="tj-sr-only">` (sem `aria-label`) e expansão inalterada. |
| 2026-09-19 | **Âmbito de conta no review (AE)**: o single trade review deixa de caminhar o journal inteiro. `openTradeDetail()` passa a levar a origem no view state (`state: { tradeId, from }`) e o `TradeDetailView` lê-a em `getState`/`onOpen`/`setState`; novo `loadScope()` filtra `allTrades` por `mappedAccount(t.account)?.id === accountId` quando a origem é uma conta (com queda para a lista completa se a alvo for leg virtual/sem conta — nunca abre vazio), pelo que as setas ‹ ›, as teclas e o contador ficam só dentro da conta; o *Reload from note* reaplica o mesmo âmbito. O cabeçalho ganha o chip `.tj-td-scope` (`N / total · <nome da conta>`, `--tj-fs-small`/`--tj-fg-3`, tooltip *Scoped to this account*) e o botão *Trade Log* — como o regresso após apagar — seguem a conta via novo `openTradeLogForAccount(id)` (espelha `openTradeLogForDay`) + `filterByAccount(id)` no `TradeLogView`, **transitório** (não grava em `settings.tradeLog`, para o ribbon continuar a abrir tudo; o chip `Account: X` mostra e limpa). |
| 2026-09-19 | **Flip card v2 e fidelidade por tipo de conta (AG)**: auditoria aos números contra o comum das prop firms (static/EOD/intraday trailing; target só na eval; consistency em fases; DLL por vezes soft; dias de passar vs dias de payout; live static) concluiu que o motor estava certo mas **não nomeava** o que calculava. O drawdown passa a ser **o da firm**: `ddToLimit` (pico − balance, com payouts) como *used*, `ddRemaining` (`balance − floor`) como *remaining*, badge por % do limite e sub com o tipo via novo `drawdownLabel()` (`lib/accountRules.ts`); saem `DD from peak` e os locais mortos `floor`/`buffer`/`ddToLimit` do `render()`. Entra **time in drawdown** (`pctTimeInDD`). `revenge` endurece: ≤15 min **no mesmo símbolo** ou `mistake` explícito após perda. Frente: saem `Winning trades` e `Revenge trades`, entram **Biggest win/loss**, `Reached 1R`, `Max-loss buffer`, `Worst day vs limit`. A barra `Risk↔Target` fica com **uma mensagem só**: o tick tracejado `.tj-acc-marker-dd` marca o floor do trailing dd e a caixa *Drawdown used* perde a segunda barra (só número, tipo e tempo em DD). A strip de distribuições (**Hold zones · Order type · Rating**) **saiu** por decisão do trader — Hold zones e Order type vão para o módulo de estratégias/playbooks; o rating fica só no verso. Ícone do flip corrigido (`arrow-left-right`, `.tj-acc-flipbtn` com `padding:0` + `svg` 15px). Verso: gauge `Mistakes` → **Clean trades**, score inclui **Strategy tagged** e diz *model*, saem `Reviewed` (só gauge) e `Hold W/L`; cores por tokens `--tj-tone-good/mid/bad`. **Cada tipo trata-se individualmente**: `resolveAccountView` resolve o programa por `phase === type` (adeus fallback `programs[0]`), o wizard semeia por tipo (eval target+EOD · funded sem target+EOD · live sem target+static · personal/demo sem regras) e os rótulos mudam a fase (*Payout winning days*). Limpeza: `getProgram`, `accountBreakdownMetric` e o CSS morto de breakdowns/widgets. Backlog ganha a linha *Tilt & psychology — limiares configuráveis*. |
| 2026-09-19 | **Conta & Accounts — cartões, General/Rules, header e Types (AI)**: segunda passagem sobre o que saiu em AH. O `splitAccountName` descarta segmentos cujas **palavras** já estejam todas no título (fim do subtítulo repetido no AMP). O título do gráfico fica só **"Equity"**. O painel Rules deixa de herdar a largura do wizard (`.tj-account-wizard.tj-as-pane { width:auto }`) — sem scroll horizontal. O **General** passa a usar os componentes do wizard: `ACCOUNT_SIZES` e `TYPE_CATALOG` (6 tipos, incl. *Other*) mudam para `lib/accountTypes.ts` e o wizard importa-os; size é dropdown com os cinco + **Custom…** (campo `$`) e type é a grelha de tiles em `--interactive-accent`; mudar o tamanho não re-semeia regras. O quadrado do header passa a **Settings** (era *Display*) e a ordem é **Copy groups · Add account · Settings**. `overflow:hidden` no `.modal-content` do Manage (scroll único). `typeOrder()` cai em `DEFAULT_ORDER` (personal · live · funded · eval · demo · unknown). Segunda passagem: tiles de tipo compactas dentro da modal da conta e ritmo vertical próprio (`.tj-acc-settings .tj-wz-field + .tj-wz-field` / `.tj-as-row + .tj-wz-field`), porque os campos ali empilham sem o `.tj-wz-row` do wizard. O nome automático segue agora o **tipo escolhido** na modal (`autoName(size, type)` usa o `selectedType`, não o `acc.type` original) — trocar a tile reescreve `Firma · Tipo · $XK` enquanto o nome estiver em modo auto. |
| 2026-09-19 | **Fundação do 2.0 — vault, IDs e migrações (AJ)**: decisões de arquitetura que **não mudam a UI do 1.0** mas tornam o 2.0 seguro. **Modelo de pastas** (novo `docs/VAULT-STRUCTURE.md`): raiz única `Tradebook/`; **evergreen** no topo (`library/strategies` · `playbooks` · `mindset`) e **série temporal** por ano (`<ano>/01..12/trades/` + `<ano>/reviews/` + `<ano>/attachments/`), sistema em `_tradebook/`; pastas criadas **à medida** (nunca vazias). Regra central: **descobrir pelo `type` do frontmatter, não pela pasta** (reorganizar nunca quebra nada). Re-homing automático (mudar o mês da data move a nota pela API do Obsidian — links preservados, idempotente, sem sobrepor nem perder). Migração 1.0→2.0 da pasta plana `Tradebook/trades/` para `<ano>/<mês>/trades/`. **`board/` descartado** (não era um pedido). **Contrato de dados** (`docs/STRATEGY-DATA-CONTRACT.md` ganha os pontos 7–11): **ID estável** para estratégias (registo `{id,name,createdAt}`; a nota guarda o nome; nome desconhecido = *untracked*, nunca perdido), **`settingsVersion` + migrações numeradas e idempotentes** (o Journalit faz o mesmo por-feature), **estratégia = nota da vault** (`library/strategies/<nome>.md`), **`tradeType: regular\|missed\|backtest` reservado**, preservar sempre campos desconhecidos do `data.json`. Risco registado: `settings.mistakes`/`settings.tags` ainda são só nomes — precisam de id se a psicologia lhes agarrar dados. Visão completa em novo `docs/STRATEGIES-2.0.md`. |
| 2026-09-19 | **Conta — nomes, cartões, cor do cash, settings e copy groups (AH)**: o **label do programa legado sai do ecrã** (o wizard grava `programId = type`; nada deve mostrar `program.label`) — `splitAccountName` usa `[firmLabel, typeLabel, $XK]` e deduplica por palavra contra o título, o título do gráfico passa a `Equity — {acc.name}`, `.tj-as-meta` e `autoName`/seed `makeAccount` ficam `firm · tipo · $XK`. **Cartões alinhados**: `.tj-acct-tile` vira flex column e a primeira barra `.tj-acct-prog.is-first` ganha `margin-top:auto`; a tag do líder ganha coroa Lucide (`crown`) e os emojis 👑/📦/🗑️ saem (Archive/Delete usam `archive`/`trash-2`). **Gráfico**: `dayCash` deixa de ser booleano e passa a `{index, kind}` — payout dourado, depósito verde, correção de fees/custo neutro (`--tj-fg-3`); o hover segue a regra (`b.tj-cost`). **Account settings**: o separador **Copy trading sai** do modal da conta (a gestão vive no Manage) com os imports e o CSS morto (`.tj-as-ov*`/`-sw`/`-copydetail`/`-copy-warn`/`-field`) apagados; o **Rules** é refeito a espelhar o wizard (target $/%, max loss $/%, daily loss, consistency, **drawdown type**, **locks**, **position size**, **min days / payout winning days**, rótulos por tipo, disclaimer) com as classes `.tj-wz-*`. **Winning days**: `AccountMetrics.winDays` + linha `X of Y days` (Y = `minDays`) em eval/funded, rotulada *Passing days* / *Payout winning days* (modelo; §0). **Copy groups**: `mountDropdown` passa a repintar label/chip/`on` no clique (corrige os 19 sítios) e `renderGroup` reorganiza-se em secções (`.tj-mg-sect`/`-sectitle`) com Change leader ao lado do líder. |
| 2026-09-19 | **Folder structure na 1.0 (AK)**: a estrutura da vault planeada em AJ passa a estar **implementada já na 1.0** — para a beta entregar a organização correta. `settings.tradesFolder` passa a ser a **raiz** (default `Tradebook`, era `Tradebook/trades`) e as notas de trade nascem em `<raiz>/<ano>/<mês>/trades/` (mês numérico; helper `tradeMonthPath` em `storage.ts`; import, Add Trade e pernas de copy seguem todos por `saveTrade`). Cada nota leva `type: trade` no frontmatter (`Trade` ganha `type?: string`; reservado `missed`/`backtest`) e a descoberta passa a ser **por `type`, não por pasta**: `loadTrades`/`collectTradeNotes` varrem a raiz recursivamente e excluem `_tradebook/` e `library/`, com compatibilidade para notas antigas sem `type` dentro de `…/trades/`. **Screenshots** vão para `<raiz>/<ano>/attachments/` e as ~5 listas de candidatos duplicadas (tradeTable, tradeDetailView ×2, tradeModal, print queue) foram **centralizadas** em `plugin.attachmentCandidates()`; os backups passam a `<raiz>/_tradebook/backups`. **Migração automática, uma vez e idempotente**: `settingsVersion` + `runMigrations()` (chamado no `onLayoutReady`) movem a pasta plana antiga para `<ano>/<mês>/trades/` pela API do Obsidian (links preservados, nunca sobrepõe, nunca perde) e acrescentam `type: trade`. Novo harness `folder-check.js` (11 asserções). Gate: build 0 · smoke 142/0 · audit só a dívida de base. |
| 2026-09-19 | **Estratégias 1.0 / Nível 2 (AL)**: a fundação escrita em AJ passa a estar **implementada**. `settings.strategies: StrategyRecord[]` (`{id,name,createdAt}`, tipo em `types.ts`) é o **registo com identidade**; a `knownSetups()` mantém a **assinatura** (devolve nomes) mas passa a fundir, por ordem, `settings.strategies` → `setups` legado → nomes das notas (case-insensitive, primeira grafia ganha). `SETTINGS_VERSION` 1→2 e nova `migrateStrategies()` idempotente (semeia o registo a partir do `setups` legado e dos nomes das notas, e escreve a nota de cada estratégia). **Cada estratégia é uma nota da vault**: `library/strategies/<nome>.md` (frontmatter `type: strategy` + `id` + `name` + `createdAt`, criada à medida e nunca sobreposta; `sanitizeFilename` limpa o nome) — as regras/documentação/readiness viverão lá. `addStrategy`/`removeStrategy` (remove só o registo, **a nota fica**) e `renameSetup` (mantém o `id`, renomeia a nota pela API, reescreve as notas de trade) substituem os antigos; `addSetup`/`removeSetup` ficam como alias. `Trade.setup` continua **string simples** (contrato §1). **Formulário pede, motor não impõe** (§0): o Add Trade manual lê `knownSetups()`, uma só estratégia é pré-preenchida, com ≥2 não há default e o *Save* fica desactivado até haver escolha, sem nenhuma o utilizador é empurrado para `＋ New strategy…`, e existe sempre a escolha explícita **No strategy**. **Import** ganha o campo **Strategy** opcional (aplica-se a TODAS as trades do ficheiro, pré-preenchido com uma só; helper honesto) e o recibo mostra `N without strategy` + botão *Assign strategies* que abre o Trade Log filtrado a essas trades (`openTradeLogForIds` + `filterByTradeIds`), com o bulk **Set strategy** por dropdown da casa. **Strategies page** cria/renomeia inline (`window.prompt` fora), mostra nomes **untracked** e avisa que remover guarda a nota. **Tutorial** ganha o passo *The strategies you trade* com Add + Skip (recomendação, nunca imposição). |
| 2026-09-19 | **Polimento da página Strategies (AM)**: a página passa a ler como **uma coluna só** (intro, cartão e nota a `max-width: 640px`; era 640/720/520). A intro encurta para "Name the strategies you trade…Each name keeps its own note in your vault." O bloco centrado **More is coming** (título grande + 4 linhas *The rule set / Trades measured / Its own numbers / One trade, one row*) sai: fica uma **nota calada** `.tj-strat-note` com o martelo Lucide (`.tj-strat-note-ico`, animação `tj-hammer` mantida, desligada com `settings.animations:false` e em `prefers-reduced-motion`) e o texto `In development — Rules, per-strategy numbers and comparison are on the way. Everything you name now carries over.` Sem contador global no cartão (a lista já mostra `N trades` por linha). CSS morto removido (`.tj-strat-title`/`-hammer`/`-list`/`-row`/`-k`/`-v`). Harness: a asserção do smoke passa a `/In development/`. |
| 2026-09-19 | **Trade Log — review honesto, escala, receita C+D e bulk completo (AN)**: a página principal do journal é reconstruída. **Review honesto** (fecha as 3 avarias parqueadas): o passo *Review* passa a ler o campo vivo (`hasText(t.notes) || hasText(t.review)`, `lib/review.ts`) em vez do `t.review` morto; a regra passa a `complete = reviewed === true \|\| (done === total && reviewed !== false)`, pelo que o motor conta os passos e o trader tem a última palavra **nos dois sentidos** (fim do clique morto no detalhe e no tick da tabela); `accountMetrics.reviewedPct` **saiu** e o gauge "Reviewed", o discipline score (`0.35·reviewCompletePct`), `main.ts`/maturity e o dashboard falam todos por `reviewCompletePct`. Rótulo do passo `Print` → **Screenshot**; o `aria-label` do dot saiu (`.tj-sr-only` + `attachTip`). **Escala**: o search espera **160 ms** e renderiza uma vez; `stats()` memoizado por chave composta (filtros + `trades.length` + `_dataVersion`); o *Load more* re-renderiza só o ledger e repõe o scroll num `requestAnimationFrame`; o haystack de busca inclui `notes` e `tags`. Novo harness `tradelog-perf.js` (2 000 trades, página a 50, re-render memoizado, 5 teclas = 1 render). **Receita C+D**: strip de 4 mini-cards (Net P&L · Win rate · Avg R · Profit factor), fila de atenção com chips (a rever / sem estratégia / sem print), ledger em card (`.tj-tl-ledger`, thead sticky por `overflow: clip`, "no strategy" âmbar), **gaveta de filtros** (`.tj-tl-drawer`, secções + Include/Exclude de contas + "Show N trades"), colunas em popover próprio. **Bulk completo** estilo Journalit: barra flutuante só com selecção (Select all · Strategy… · Rating… · Account… · Mark reviewed · More… com unreviewed/mistake/tag/duplicate/delete · Clear), **zero `window.prompt`** (input inline para `＋ New strategy…`), **tags por união** via novo `updateTradeTags` (o `updateTradeFields` estragava o array), delete por **modal da casa** (`ConfirmModal`), **Shift-clique** de intervalo e **Cmd/Ctrl+A**. |
| 2026-09-19 | **Receita canónica de superfície (AO)**: a variante escolhida a 19 Set para o Trade Log (C · Híbrido editorial) passa a ser **norma**, não estilo de página. `UX-GUIDELINES.md` ganha **§6.1 "One surface language, for every page"** (números numa linha fina com `(i)`, listas num painel sem cartão, fila em quiet chips, hairline perceptível, máximo ghost) + um item na checklist §7; `UI-CATALOG.md` ganha **§6.0 "Receita canónica de superfície (normativa)"** (tabela peça→classes→regra + proibidos: `#d9a441` literal, `background-secondary` como fundo de página, `box-shadow` decorativo, cor sem palavra). No **Trade Log**: `.tj-statline/-cell/-k/-v/-s` substitui os 4 mini-cards (um `(i)` por figura), `.tj-attention`+`.tj-quietchip` substitui os chips-pill, `.tj-panel/-head/-title/-count/-note` substitui o cartão cinzento do ledger, a gaveta passa a coluna com hairline (sem fundo/raio) e a barra de bulk passa a tinta de acento; sai o fallback falso `var(--fg-3)` e o `#d9a441` literal; CSS morto removido e a classe órfã `tj-tl-ledgerwrap` fora do view. Backlog: alinhar **Accounts/Home/conta** à receita na passagem seguinte. Gate: build 0 · smoke 142/0 · audit só dívida de base · folder-check OK · tradelog-perf OK. **Revertido no mesmo dia (AP):** a receita canónica passa a ser a **variante A (linguagem da página Accounts)** — ver linha seguinte. |
| 2026-09-19 | **Trade Log — linguagem A, mecanismos e acessibilidade (AP)**: o trader preferiu a **variante A** («fica mais limpo») e ela passa a **lei**. **Norma reescrita**: `UX-GUIDELINES.md` §6.1 e `UI-CATALOG.md` §6.0 passam de «stat line + plain panel + quiet chips» (C) para a linguagem **Accounts** (faixa de números num só cartão segmentado `.tj-acct-strip`; listas em cartão translúcido raio 14; cabeçalhos `.tj-acct-h1` com ponto+label+contagem+hairline; fila em `.tj-attn-chip`; gaveta como cartão irmão). Dívida registada: o Trade Log adotou A; **Home e página da conta** alinham depois. **Trade Log em A**: cabeçalho `.tj-acct-header`/`-header-actions` (fora os remendos `.tj-tl-head`), strip `.tj-acct-strip*`, atenção `.tj-attn-chip`(+`.is-on`)/`.tj-attn-none` (zero `#d9a441` literal), ledger `.tj-panel` cartão + cabeçalho `.tj-acct-h1`, `.tj-panel-note`; chips de filtros activos em pill da casa; busca e gaveta e bulk como cartões; Load more e Cancel do confirm em ghost (`.tj-actionbtn`); `.tj-statline*` e `.tj-quietchip*` saem. **Selects**: os 5 `<select>` nativos da gaveta passam a **dropdown da casa** (`mountDropdown`), morrendo o `disabled` morto do `sel()`. **Mecanismos/bugs**: `filterByAccount`/`filterByDay`/`filterBySetup`/`filterByReview` tornam-se **lentes transitórias** (`_skipPersist`, respeitando AE); persistência só quando a assinatura muda (fim do save storm a cada render); `idFilter` ganha chip visível com clear e repõe o período; **Cmd/Ctrl+A respeita os filtros**; os scoped opens esperam pela view (`scopedTradeLogView()` → resolve o `DeferredView`); `groupFilter` mostra o nome; `__none__` mostra "None"; `accountExclude` e a selecção aparecem em chip; o delete em lote di-lo honestamente (trash do Obsidian vs permanente); `Clear all` limpa `idFilter`/`accountExclude`/`accQuery`; a memo key de `stats()` inclui `includeCopiesInPortfolioAnalytics` e `timeZone`; `hasCopies` por `isCopiedTrade`; novo `onClose` limpa os timers. **Acessibilidade**: sort no header por teclado (`th` focável + Enter/Space) e **setas** para reordenar colunas (alternativa ao drag, SC 2.5.7); `aria-label` retirado das âncoras de `attachTip` (Trade Log, tabela, painel de contas, header das contas) — o nome acessível viaja num `.tj-sr-only`; comentário do `tip.ts` alinhado. **Limpeza**: fora os campos mortos de `tradeLog.filters` (`type`/`duration`/`tag`/`tags`/`limit`), o `savedLedgerY`, as chaves `LEGACY_KEYS` não lidas, e o CSS morto (`tj-tbl-shot*`/`tj-tbl-noshot*`, `tj-pop-row`, `tj-filterbtn-clear`, `tj-filterbar-compact`, selectors duplicados), com `.tj-tl-colmove*` novo e `.tj-tl-activechip-x` a 24×24. Gate: build 0 · smoke 142/0 · audit só dívida de base · folder-check OK · tradelog-perf OK. |
| 2026-09-19 | **Tabela de trades em linguagem A + painel na página da conta (AQ)**: o trader pediu — «agora temos de mudar a parte onde as Trades aparecem, ainda tem o look antigo, não encaixa com isto novo que estamos a criar». O **ledger partilhado** (`lib/tradeTable.ts` + CSS `.tj-tbl*`) deixa o look antigo: as células passam de `var(--text-muted)` do tema para **`--tj-fg-3`** (o token AA da casa, no `tbody td`, nas linhas de fill, na tag de fill e no `.tj-flat`); o **hover da linha** deixa `var(--background-modifier-hover)` e usa o translúcido da casa `rgba(255,255,255,.035)`; o **cabeçalho do dia** perde o fundo opaco (`--background-primary`) e a pill do tema — fica transparente, com `border-bottom` em `var(--background-modifier-border)`, a pill em `rgba(255,255,255,.05)` + hairline e a régua no token; as **bordas das linhas** deixam de ser assimétricas (topo 100% / fundo 55%) e sobra uma hairline por cima de cada linha; o **header da tabela** perde o `border-radius: 7px` que o fazia uma barra-pílula (fica quadrado, como o painel que o segura) mantendo o `box-shadow` funcional do sticky. As **regras duplicadas** do dia (dois `.tj-tbl-day td`, dois `.tj-tbl-daymeta`) foram unificadas, o `padding` morto da pill saiu, e os literais de estado passaram a tokens: estrelas `#f5b301` → **`--tj-tone-mid`**, e `#d9a441` → `--tj-tone-mid` no `.tj-tbl-review` e no `.tj-tbl-pnl-partial`; o `.tj-tbl-accs` (contagem de contas com tooltip) fica em `--tj-fg-3` com o tracejado no mesmo tom. **Página da conta**: a lista de trades deixa de ser uma tabela nua — é o mesmo **`.tj-panel`** do Trade Log (cartão translúcido raio 14) com o cabeçalho **`.tj-acct-h1`** (ponto · `Trades` · contagem em pill · hairline · o chip de filtro da breakdown quando existe) e a contagem a acompanhar o filtro; o título do widget passa a `Trades in this account` (o número vive no cabeçalho do painel) e o `.tj-acc-tradeshead` morto saiu do CSS. Gate: build 0 · smoke 142/0 · `ux-audit` só a dívida de base (contrast 0; 3 px off-scale; 5 alvos pequenos) · folder-check OK · tradelog-perf OK. Deploy: `main.js 03e6849c5da6c0e08d74679fd394d91e`, `styles.css 05c4c547071b1027cfb57182dbf42895`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`. |
| 2026-09-21 | **Trade Log — streamlining: strip out, inline summary, overlay drawer, bulk refactor, columns rewrite (AS)**: refactor de limpeza e densidade. **Strip removido** (4 statCell Net P&L / Win rate / Avg R / Profit factor) e substituído por **resumo inline** no cabeçalho do ledger (`fmtMoney(net) · winRate% win`, com tone `.tj-pos`/`.tj-neg`); `TradeLogStats` perde `avgR`/`profitFactor`; `statCell` method removida. **Gaveta vira overlay slide-over** (`position: absolute`, `z-index: 10`, backdrop semi-transparente, Escape fecha); `.tj-tl-shell` ganha `position: relative`; a tabela fica sempre a 100% de largura. **Bulk bar refactorizada**: Account picker dropdown e More... submenu saem; ações expostas diretamente (Strategy, Rating, Mark reviewed, Add mistake, Add tags, Duplicate, Delete); `bulkAccount` method removida; Mark unreviewed removido. **Columns popover reescrita**: grelha de 2 colunas com setas é substituída por **lista vertical** (`.tj-tl-collist`) com handles de drag-and-drop (`⋮⋮` grip-vertical) + checkboxes de visibilidade; drag por mousedown/mousemove/mouseup com estados `.dragging`/`.drag-over`; presets (Default/Simple/Reset) e toggle Side mantidos; nota final atualizada. **CSS limpo**: `.tj-tl-colmove*` (setas) e `.tj-tl-cols` (grid) removidos; novas classes `.tj-tl-collist`/`.tj-tl-colitem`/`.tj-tl-colhandle`/`.tj-tl-colcheck`/`.tj-tl-collabel`. **Harness atualizado**: `filters-check.js` (Account picker assertion removida), `tradelog-perf.js` (strip assertion → inline summary). Gate: build 0. |
| 2026-09-21 | **Trade Log — preset layouts, compact date, icon print, column simplification (AT)**: limpeza e alinhamento com o mockup. **Preset Default** = `[date, symbol, side, qty, entryexit, points, pnl, setup, stars, image]`; **Simple** = `[date, symbol, side, qty, entryexit, points, pnl]`. **Coluna Review removida** de `TRADE_COLUMNS` (já não existe na tabela); `onToggleReviewed` sai de `TradeColumnCtx` e `TradeTableOpts`; `reviewStatus` import removido de `tradeTable.ts`. **Print vira ícone clicável** (`setIcon(image)` + `cursor:pointer` + click → `openTradeDetail`), sem texto; tooltip muda para "Click to open the trade and see it.". **Date coluna compacta** — novo `compactDateLabel()` em `tradeTable.ts` (formato "DD Mon" sem ano) + opção `compactDate` no `TradeColumnCtx`; o Trade Log passa `compactDate:true` e `showDayHeaders:false`, eliminando os divisórios de dia (`.tj-tbl-day*`); a coluna mostra "14 Sep, 09:57". **Opções novas** em `TradeTableOpts`: `showDayHeaders` (default true) e `compactDate` (default false); quando `showDayHeaders:false`, o bloco `if (opts.groupByDay && opts.showDayHeaders !== false)` é saltado — o sorting por dia mantém-se mas as cabeceiras de dia desaparecem. **CSS morto removido**: `.tj-tbl-review` e `.tj-tbl-tick*`; o CSS dos day headers `.tj-tbl-day*` mantém-se (o código de renderização continua presente para consumidores que queiram agrupar por dia). **Gaveta**: spacing `.tj-tl-dsec` sobe para 20px (era 14), `.tj-tl-drawerbody` padding 8px 0 16px (era 6px 0 12px). Gate: build 0 · smoke 142/0 · audit só dívida de base. Deploy: `main.js fbcdd23d` + `styles.css acd56ab9`. |
| 2026-09-21 | **Trade Log — table container shrinking fix (AV)**: dois fixes de CSS para o bug em que a abertura dos active filter chips (chip de atenção) fazia a tabela encolher para a esquerda com espaço vazio à direita. **`.tj-tl-sub` ganha `min-width: 0`** — impede que o texto longo dos chips activos (ex. "Missing: no print or no strategy") force um min-width no flex chain que empurra o layout para além do container. **`.tj-tl-shell` ganha `width: 100%`** — garante que o shell da tabela preenche sempre a largura total do parent, independentemente do overflow de siblings. Gate: build 0 · smoke 142/0. Deploy: `main.js efbe524d` + `styles.css 42aea28c`. |
| 2026-09-21 | **Trade Log — fixes: attention pills, columns popover sync, drawer spacing, side toggle (AU)**: 4 bugs corrigidos. **Attention pills**: `pending`/`missingSetup`/`missingPrint` passam a ser calculados a partir de `filtered({ skipAttention: true })` — a lista ignorando `reviewFilter` e `qualityFilters` — para que clicar num chip nunca mude o número que ele próprio displays (bug: o count mudava por causa da dedup de copy trades no subconjunto filtrado). **Columns popover sincronizado**: o popover itera `tradeLogColOrder` (ordem completa de todas as colunas) em vez de `TRADE_COLUMNS` (ordem canónica); presets, drag e toggle de visibilidade escrevem em `tradeLogColOrder` via novo `syncSharedOrder()` para manter a tabela e o popover sempre espelhados; o `onReorder` da tabela preserva colunas ocultas na ordem partilhada. **Drawer spacing**: `.tj-tl-opts + .tj-tl-opts` ganha `margin-top: var(--tj-sp-2)` (8px entre rows de pills na mesma secção); `.tj-tl-optnone` ganha `padding: 3px 0`. **Side toggle**: `.tj-wz-seg-opt` ganha regra base não-scoped (antes só existia sob `.tj-account-wizard`/`.tj-acc-settings`); o toggle de Side no columns popover agora mostra o estilo underline correto. Gate: build 0 · smoke 142/0 · audit só dívida de base. Deploy: `main.js efbe524d` + `styles.css 93d306ef`. |
| 2026-09-21 | **Trade Log — rating column width fix + attention pills ghost style (AW)**: dois fixes de polish. **Rating column**: a coluna Rating passa a ter `min-width: 4.5em` via `.tj-tbl-stars` (`display: inline-block`), e o render sempre cria o span `.tj-tbl-stars` (mesmo para o `—` de unrated) — elimina o layout shift entre linhas com e sem estrelas. **Attention pills**: os chips de atenção passam a estilo ghost outlined (sem fill âmbar no `.is-on`), alinhados ao padrão dos botões de header (Filter/Select/Columns); hover usa `--background-modifier-hover`, active usa só border âmbar sem background tinted. Gate: build 0 · smoke 142/0. Deploy: `main.js e375abf6` + `styles.css cb5f705b`. | | 2026-09-19 | **Trade Log — gaveta em pills, bulk só com palavras, mockup A cumprido (AR)**: o trader — «ainda não ficou igual ao mockup que foi criado, eu queria dessa maneira para depois continuar» — mandou reproduzir a **variante A** de `previews/tradelog-revamp-v1.html`. Decidido no mesmo passo: a coluna **Review fica com o tick ✓/○** («ainda não sei como vamos implementar essas cenas para o automatic review system») e a **gaveta é literalmente como o mockup**. **Gaveta** (`tradeLogView.ts`): os helpers `sel()`/`multi()` saem e entram `section()` (`.tj-tl-dsec`/`-dsec-t`) e `pills()` (`.tj-tl-opts` → `.tj-tl-opt`(+`.on`)/`.tj-tl-optnone`); What I traded (account picker + par **Include/Exclude** em `.tj-tl-incl` + ticker), How it went, When e Was it done properly em linhas de pills; o rodapé ganha **`.tj-tl-drawer-show`**. **Bulk bar** só com palavras: o `act()` deixa de tomar ícone, `.tj-tl-bulkbtn` fica em `--tj-fs-small`, os pickers ganham o aspecto `prim` (`.tj-tl-bulkpick .tj-mg-dd-btn` com borda) e o cartão passa a `rgba(255,255,255,.022)` + borda `color-mix(accent 35%, border)` + raio 10. **Barra**: `.tj-tl-periodbar` vira segmented control, a busca fica numa hairline por baixo, os chips activos passam a texto sublinhado (o × mantém 24×24) e `.tj-attn-chip` ganha o **ponto âmbar de 6px** por `::before` (27px, realce no `:hover`/`.is-on`). **Cabeçalho do dia**: réguas de largura total → **hairlines curtas de 26px**, pill sem fundo/borda, rótulo a `--tj-fs-label` e o contador como tag (raio 5, `rgba(255,255,255,.07)`). **Limpeza**: fora `.tj-tl-picks`/`.tj-tl-pick`(+`:hover`/`.on`), `.tj-tl-bulkbtn .tj-btn-icon svg` e `.tj-attn-chip .tj-btn-icon svg`; `.tj-tl-picknone` fica. Gate: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 5 alvos pequenos) · folder-check OK · tradelog-perf OK. |
| 2026-09-21 | **Trade Log — attention chips como botões de header, limpeza de listeners/memória e o cabeçalho do ledger a sobreviver ao redraw (AX)**: polish + saúde de código. **Attention chips** (`styles.css`): `.tj-attn-chip` deixa o look de pílula (`border-radius: 999px`, `min-height: 27px`, `padding: 0 11px`) e passa a **botão ghost do header** — `border-radius: 6px`, `padding: 4px 10px`, `border: 1px solid var(--background-modifier-border)`, fundo transparente; mantém o ponto âmbar de 6px (`::before`). **Bug real corrigido**: `renderLedger` desenhava o `(i)` cabeçalho do cartão (o resumo inline `fmtMoney(net) · winRate% win` e a nota "copies shown once") **dentro** do mesmo `.tj-panel` do `renderTradeTable`, mas o `draw()` de `tradeTable.ts` começava por `host.empty()` — **apagava o cabeçalho em todos os redraws**, pelo que o resumo nunca chegava ao ecrã desde a AS. Agora `draw()` limpa **só as tabelas que ele próprio criou** (`for child of children: if TABLE remove`), e o cabeçalho sobrevive. **Listeners/memória** (`tradeLogView.ts`): o `keydown` do Escape da gaveta passa a ser libertado em **todos** os caminhos de fecho (novo bloco no topo do `render()` quando `!filtersOpen`); o drag do popover de colunas ganha `_dragCleanup` (mousemove+mouseup+blur) e o timeout de foco do input inline passa a `_focusTimer`; `onClose()` limpa `_searchTimer`/`_filterTimer`/`_focusTimer`, `_drawerKeyCleanup`, `_dragCleanup` e nula `_stats`/`_bulkHost`/`_ledgerHost`. **Código morto removido**: `markReviewed` e `filterBySetup` (tradeLogView); `holdMinutes` (export), `TradeColumn.groupedLabel`, `TradeTableOpts.collapse` e o retorno closure de `renderTradeTable` (passa a `void`) em `tradeTable.ts`. **CSS**: removido o bloco duplicado verbatim `.tj-account-wizard/.tj-acc-settings .tj-wz-seg-opt` (fica a base não-scoped). **Tipos**: `accountDashboard.ts` importa `DEFAULT_ACCOUNT_ORDER` e `TradeSort` de `lib/tradeTable` (fim da duplicação `ACCOUNT_TABLE_COLUMNS`). **Harness**: `smoke.js` asserta `filterBySetup` → passa a `filterByTradeIds`; `tradelog-perf.js` asserta o resumo inline. Gate: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 5 alvos pequenos) · tradelog-perf OK. Deploy: `main.js 9913c5bcff9be6fcdf39c5f12f8761a2` + `styles.css 7ea6509bf5cc5106783eb4ace5c53956` + `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`. |
| 2026-09-21 | **Trade Log — chips sem fill e o resumo do cabeçalho fora (AY)**: a pedido do trader, só correções visuais. **`.tj-attn-chip`** passa a **totalmente transparente**: `background: transparent !important` + `background-image: none !important` (o `!important` vence o fill/gradiente que o Obsidian pinta nos `button`) e o `background` do `:hover` sai (não podia vencer o `!important` da base — era CSS morto); a borda continua sem `!important` para que o estado `.is-on` a possa tingir de âmbar. **Resumo do cabeçalho do ledger removido**: sai o bloco `if (s.tradeCount > 0) { … .tj-acct-h1-summary … }` do `renderLedger` (o `+$X · NN% win` à direita de "TRADES N"), e o cabeçalho fica `dot · Trades · pill da contagem · hairline · [nota "copies shown once"]`. Sem CSS morto (não existia regra para `.tj-acct-h1-summary`). **Consequência**: `net` e `winRate` saem de `TradeLogStats` + `stats()` (só o resumo os lia) e o import `fmtMoney` sai do `tradeLogView.ts`. Gate: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 5 alvos pequenos). Deploy: `main.js b1751ba869d00f227677a1c96c1b52dc` + `styles.css 637e85a155747a5c48d1e3ba1000bce9` + `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`. |
| 2026-09-21 | **Trade Log — cabeçalho do ledger removido (AZ)**: a pedido do trader, a barra `.tj-acct-h1` acima da tabela (`dot · TRADES · pill da contagem · hairline`) sai por completo do Trade Log — era redundante numa vista dedicada e o "TRADES" aparecia cortado ao topo. `renderLedger` passa a criar só o `.tj-panel` e a chamar `renderTradeTable`; a tabela fica diretamente abaixo da barra de pesquisa/tempo (o `.tj-panel` mantém `margin-top: 14px` e o `thead` os seus `8px`, pelo que o topo fica alinhado). **Consequência**: sai também a nota "copies shown once" (vivia nesse cabeçalho) e `records`/`hasCopies` saem de `TradeLogStats` + `stats()`; CSS morto removido (`.tj-panel .tj-acct-h1`, `.tj-panel-note` — `.tj-acct-h1` continua a servir a página de Contas). **Harness**: `tradelog-perf.js` passa a assertar `ledger table and attention queue rendered`. Gate: build 0 · smoke 142/0 · tradelog-perf OK · audit só dívida de base. Deploy: `main.js 3689f6bb910de964df7442368bf59f38` + `styles.css 1737c29cf1b638a19e20f7bee82d92e7` + `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`. |
| 2026-09-21 | **Fundação das tags — Grupo 1 (BA)**: persistência de `psychology_tags`/`mistake_tags` escrita em YAML nativo, sem `JSON.stringify`. `Trade` ganha os dois campos (`types.ts`). `storage.ts` ganha `inlineArray(values)` (helper de `string[]` — `updateTradeFields` citava tudo e transformava uma lista num scalar), `tradeToMarkdown` passa a emitir **sempre** as três chaves de array (`tags`, `psychology_tags`, `mistake_tags`) para o esquema ser estável, e `parseTradeFromMarkdown` lê com fallbacks não-destrutivos: `psychology_tags` presente → ele próprio, ausente → `tags` legado; `mistake_tags` presente → ele próprio, ausente → `[mistake]` se não vazio (deteção estrita de presença por regex sobre o frontmatter). `updateTradeTags` (código morto, zero callers) é substituído por `updateTradeArrayFields(app, file, Record<string,string[]>)`; novos `updateTradeScreenshots` (reescreve o **bloco** `screenshots:` — corrige a perda de prints do bug do JSON-scalar) e `setTradeMistakeTags` (fonte única do dual-write: escreve `mistake_tags: [...]` **e** espelha `mistake: "<tags[0] || \"\">"`). `buildLeg` propaga os dois arrays nas pernas de copy. Novo módulo puro `lib/tags.ts` (`normalizeTag`/`normalizeTags` — trim, colapso de espaços, dedupe case-insensitive, primeira grafia ganha). Funções puras; **sem migração destrutiva** dos `tags` legados. Gate: build 0 · smoke **142/0** · `fills-check.js` +8 asserções de round-trip (as 2 falhas restantes são pré-existentes: badge de qty e "plain trade gets no Executions section"). Sem UI nova neste grupo. |
| 2026-09-21 | **Review do Trade — Grupo 2: chips de tags + autosave debounced (BB)**: o `TradeDetailView` ganha dois **contentores de chips** — *Psychology State* (`psychology_tags`) e *Execution Mistakes* (`mistake_tags`) — no lugar da linha `Tags` editável. Cada contentor mostra as tags escolhidas (chip com ×, 24×24) mais sugestões tracejadas com `＋` das labels já usadas no journal (uma só grafia, `normalizeTags` deduplica case-insensitive). Adicionar é **inline** (`＋ Add tag` → input, Enter commita, Escape cancela; **nunca** `window.prompt`). Rating 1–5 mantido (estrelas com `tj-td-star-glyph` + `.tj-sr-only`, clique na mesma estrela limpa). **Autosave debounced** novo: `debounceReview(key, run, ms)` + `flushReviewSaves()`, com timers por campo (`notes` 600ms, `rating` 400ms, cada `_tags` 600ms); o objeto `Trade` é atualizado já (UI não espera pelo disco) e a nota acompanha a seguir; flush em `setTrade` (trocar de trade) e `onClose` — sem perder o que estava a meio. Persistência: `psychology_tags` → `updateTradeArrayFields`, `mistake_tags` → `setTradeMistakeTags` (dual-write do `mistake` legado). CSS novo: `.tj-td-tagchips/-tagchip/-tagx/-tagempty/-tagsug/-tagadd/-tagaddbtn/-taginput/-star-glyph`. Gate: build 0 · smoke **142/0** · audit só dívida de base (3 px off-scale, 5 alvos pequenos; `.tj-td-tagx` a 24×24, sem violação nova). |
| 2026-09-21 | **Review do Trade — Grupo 3: tooltip multi-contas + carrossel de prints (BC)**: o badge `👥 N accounts` sobe para o **header** do Trade Detail (entre os pontos de review e as ações) — `renderAccountBadge(host, t)` agrupa por `legBaseKey`, esconde-se com <2 registos, e abre um popover `.tj-td-accpop` com a tabela **Account · Qty · Avg Entry · Net P&L** (originais primeiro, tag `original`, qty/fillSet, `toneClass` no P&L) e a nota "your trading numbers count this trade once". O antigo `renderAccountsBlock` (bloco no corpo) e o seu CSS (`.tj-td-accrows`/`-accrow`/`-accname`/`-accpnl`) **saíram**. O mosaico de screenshots dá lugar a um **carrossel**: `tj-td-shot-main` (imagem grande + overlay *Annotate*) + **faixa de labels** com quick-tags (`1m`/`5m`/`15m`/`HTF`) e input de label + **thumbnails** com borda de acento na ativa, label e remover (24×24). Nova `_activePrint` (reset por trade). A persistência deixa o `JSON.stringify` e passa por **`updateTradeScreenshots`** (bloco YAML nativo, corrige a perda de prints no reload); `handleScreenshotFile`/`removeScreenshot`/`setPrintLabel` usam `saveScreenshotsNow()`. Dropzone passa a "+ Add print". CSS morto removido (mosaico v5, `.tj-td-shot-wrap/-note`); CSS novo `.tj-td-accbadge*`/`.tj-td-accpop`/`.tj-td-acctable*`/`.tj-td-shot-carousel|main|labels|lab|labelinput|thumbs|thumb*`. Harness: `__tjStorage` expõe `updateTradeScreenshots`/`updateTradeArrayFields` e `fills-check.js` ganha **10 asserções** (substitui bloco, sincroniza o escalar legado, cura um `screenshots` escalar-JSON, insere bloco quando ausente, fence intacta). Gate: build 0 · smoke **142/0** · fills-check +10 ok · audit só dívida de base. |
| 2026-09-21 | **Review do Trade — layout em cartões, fiel ao mockup 1.html (BD)**: o `TradeDetailView` deixa a lista vertical plana e passa a **cartões**. Novo helper `panelCard(host, title)` (`.tj-td-panel`/`-head`/`-title`/`-body`). A coluna esquerda vira **rail fixo** (`grid-template-columns: minmax(320px,440px) minmax(0,1fr)`, quebra a 900px) com dois cartões: *Execution & Risk* (P&L hero, pontos, R, hold, entry/exit time, entry/exit, contratos, símbolo, direção, stop, target, R:R, risco $, fees, order type, sessão, max position) e *Critical Review & Psychology* (Reviewed → Strategy → Rating → chips de tags → notas). A coluna direita é o cartão *Screenshots & Visual Analysis* com contagem no título. **Screenshots**: as quick-tags `1m/5m/15m/HTF` **saem** (só o input livre "Name this print…"); o cabeçalho passa a *Annotate* + novo *Enlarge* (`openPrintLarge` — lightbox `.tj-td-lightbox` `position:fixed`, fecha com clique/Escape, `_lightboxCleanup` limpa o listener e o `onClose` também); a imagem principal fica contida (`object-fit:contain`, wrapper escuro de 380px, `overflow:hidden`) e é clicável para ampliar; as thumbnails passam a **strip horizontal** (`flex-wrap:nowrap`, `overflow-x:auto`) e o **+ Add Print** deixa a barra full-width e passa a **tile quadrado tracejado** no fim da strip (`.tj-td-shot-add`, 104px, `border:1px dashed`). **Tags**: `knownTags()` semeia defaults antes das labels já usadas (dedupe case-insensitive) — mistakes `Hesitation Entry · Early Exit · FOMO · Moved Stop · Overleveraged`, psychology `Confident · Anxious · Impatient · Revenge · Disciplined`; são **só sugestões** (nunca se escrevem sozinhas), clicáveis para ligar/desligar, com o input inline a aceitar novas. CSS morto removido: `.tj-td-stats/-stat*`, `.tj-td-dropzone-card/-header/-title`, `.tj-td-dropzone.compact`, `.tj-td-dropzone-add-label`, `.tj-td-review-card*`, `.tj-td-rating-section`, `.tj-td-tagempty`, família flip-card (`.tj-td-flip-card/-face/-ffront/-fback/-flipbtn`), `.tj-td-shot-lab*`, legado v4/v5 e `@keyframes tj-pulse`/`tjFlipIn` (trade detail). As 3 regras `.tj-app .tj-td-cols > *` passaram a `.tj-app .tj-td-panel`. Gate: build 0 · smoke **142/0** · audit só dívida de base (contrast 0; 3 px off-scale; 4 alvos pequenos) · deploy vault-dev md5 ok. |
| 2026-09-21 | **Review do Trade — densidade e barra-hero, fim do scroll infinito (BE)**: a queixa do trader («bloated vertically… endless unnecessary scrolling… columns misaligned») era **CSS, não estrutura**. Entra uma **barra-hero** horizontal no topo do corpo (`.tj-td-hero`): à esquerda o título (`SYMBOL · Long/Short` + `data, hora` + chip da estratégia, `.tj-td-hero-sym/-when/-badge`), à direita três métricas (`Hold Time · Points · Net P&L`, `.tj-td-hero-metrics/-metric/-k/-v` com tons `.pos`/`.neg`) — e as três linhas correspondentes **saem** do cartão Execution & Risk (o R-Multiple fica). O grid de duas colunas passa a `gap:20px`, `align-items:start` com `.tj-td-right { align-self:start }` e o empilhamento sobe para `960px` (fim da leitura de "colunas desalinhadas" em painéis estreitos). **Densidade**: `.tj-td-panel` padding 16→14, `.tj-td-panel-head` margin 12→8, `.tj-td-flip-row` padding vertical 7→4, `.tj-td-field` margin 16→10 e label 8→6, `.tj-td-textarea` padding 8px 10px + min-height 80→64, `.tj-td-tagchip`/`.tj-td-tagsug`/`.tj-td-tagadd` mais justos, estrelas inline 26→24, `.tj-td-shot-carousel` margin-bottom 0, labels 10→8, thumbs 12→10, `.tj-td-head` margin 20→14 + padding 16→12. **Execuções**: os stat-cards (`.tj-td-execs-cards`/`.tj-td-execcard*`, ausentes do mockup) **saem** do `renderExecutions` e do CSS, com o `margin` do bloco a 14px; a tabela continua full-width sob o grid. Sem código morto. Gate: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 4 alvos pequenos). Deploy vault-dev md5 ok (`main.js b9d16ed1`, `styles.css 698bc82e`, `manifest.json 4395b22f`), `data.json` intacto. |
| 2026-09-21 | **Review do Trade — cartões v2: hero com badges, tags toggle, execuções (BF)**: segunda passagem de fidelidade ao mockup `previews/1.html`. **Hero**: `.tj-td-hero-sym` passa a ser só o símbolo; entram o badge de direção `.tj-td-hero-dir.is-long/.is-short` (verde/vermelho por token) e o badge de estado `.tj-td-hero-status` (âmbar *Needs Review* → verde *Reviewed*, clicável, com `attachTip` — nunca `aria-label`); `.tj-td-hero-title` alinha ao centro com wrap e `.tj-td-hero-metric` alinha ao fim. **Reviewed sai do cartão**: a linha `Reviewed` do *Critical Review & Psychology* desaparece — o estado vive no badge do hero (o cartão começa na Strategy), e `reviewState` passa a ser lido no hero. **Tags toggle**: `tagSection` desenha **um só chip por label** a partir de `normalizeTags([...knownTags(key), ...tags])`, com estado `.is-on` e clique a ligar/desligar; as sugestões tracejadas `.tj-td-tagsug` (com o `::before` "＋") e o botão × `.tj-td-tagx` saem do código **e** do CSS; os defaults (5 mistakes do mockup — Hesitation Entry · Early Exit · FOMO · Moved Stop · Overleveraged — e 5 psychology — Confident · Anxious · Impatient · Revenge · Disciplined) ficam sempre visíveis como sugestões desligadas, e o input inline `＋ Add tag` continua a aceitar tags novas. `.tj-td-tags--mistakes` tinge o `.is-on` de vermelho. **Execuções**: `renderExecutions` passa a ser um cartão da casa (`.tj-td-panel.tj-td-execs` + `.tj-td-panel-head` + `.tj-td-panel-title`, adeus `h3`) e os badges de fill ganham tinta por lado (`.tj-td-execrow.is-entry .tj-tbl-fill-tag` verde, `.is-exit` vermelho, `.is-flat` muted por último); `margin` do bloco a `16px 0 4px`. **Grid**: base `minmax(320px,440px) minmax(0,1fr)`; nova faixa intermédia `@media (max-width:1399px) and (min-width:901px)` com `minmax(300px,360px) minmax(0,1fr)`; stack a `900px` (o bloco antigo a 960px sai). **Viewer**: `.tj-td-shot-main` passa a `height: clamp(260px, 32vh, 380px)`. Sem código morto (verificado por `rg`). Gate: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 4 alvos pequenos). Deploy vault-dev md5 ok (`main.js d6b827af`, `styles.css cc154c34`, `manifest.json 4395b22f`), `data.json` intacto. |
| 2026-09-21 | **Review do Trade — harness `tradedetail-check.js` e a estrutura que ele prova (BG)**: novo harness de 27 asserções em `~/trading-journal-smoke/tradedetail-check.js` (jsdom + `main.js` copiado + `styles.css` injetado) que corre contra o DOM real e o CSS real. Provou e fechou quatro desvios: o hero passou a vestir os badges com as classes que o mockup nomeia (`.tj-td-hero-badge.is-long/.is-short` para a direção, `.tj-td-hero-badge.is-status.is-needs-review/.is-reviewed` para o estado de review — o antigo `.tj-td-hero-status.is-done` saiu); as linhas **Entry Time** e **Exit Time** saíram do `execCard` (o tempo vive no hero, com `zoneShortLabel` fora do import); **Target** e **Order Type** foram dobrados num `<details class="tj-td-more">` no fim do cartão, com `summary.tj-td-more-sum` e `.tj-td-more-body`; e o palco de prints ganhou o `.tj-td-shot-badge` (etiqueta escura absoluta no canto, como o `.screenshot-badge-label` do mockup). CSS: base `.tj-td-cols` a `440px minmax(0,1fr)`; thumbnails passam de flex a `grid repeat(auto-fill, minmax(110px,1fr))` (o tile `+ Add Print` perde `width:104px`/`flex:0 0 auto` e flui na grelha); a thumbnail ativa passa de acento a **verde** (`--color-green-bright, #34d17a`). Harness corre **27/27 PASS**; gate completo: build 0 · smoke 142/0 · audit só dívida de base (contrast 0; 3 px off-scale; 4 alvos pequenos). Deploy vault-dev md5 ok (`main.js 0bfeb891`, `styles.css 39c4aa20`, `manifest.json 4395b22f`), `data.json` intacto. |
| 2026-09-21 | **Trade Detail v2 — layout responsivo com 3 breakpoints, hero como cartão, chips de tags com toggle ligado/desligado, grelha de thumbnails nomeadas, alturas clamp() (BI)**: o `TradeDetailView` fecha a passagem ao mockup `previews/1.html` com responsividade a sério e com o harness que a prova. O **hero vive no corpo** (não no header sticky) como cartão — símbolo, data/hora, badge de direção (`.tj-td-hero-badge.is-long/.is-short`), badge de estado de review (`.is-status.is-needs-review/.is-reviewed`, clicável) e **exatamente 3** `.tj-td-hero-metric` — pelo que o header fica só com navegação e ações e não conta altura fixa em ecrãs pequenos. O grid `.tj-td-cols` passa a **três breakpoints**: ≥1400px rail `440px minmax(0,1fr)`; `900–1399px` rail `minmax(300px,360px) minmax(0,1fr)`; `<900px` uma coluna **empilhada por prioridade** (hero → screenshots → review → execução → fills). As alturas grandes usam `clamp(min, Nvh, max)` — `.tj-td-shot-main` a `clamp(260px, 32vh, 380px)` — nunca px fixos. **Entry Time**/**Exit Time** saem do cartão (vivem no hero) e **Target**/**Order Type** são dobrados num `<details class="tj-td-more">`. Os **chips de tags** ficam com defaults semeados sempre visíveis e **toggle ligado/desligado** (`.tj-td-tagchip` + `.is-on`, mistakes por `.tj-td-tags--mistakes`), mais `＋ Add tag` inline. As **thumbnails** passam a grelha nomeada (`repeat(auto-fill, minmax(110px, 1fr))`) com a ativa contornada a verde (`--color-green-bright, #34d17a`), `.tj-td-shot-badge` no palco e o tile `+ Add Print` tracejado a fluir na grelha; as execuções ganham badges por lado (ENTRY verde / EXIT vermelho / break-even muted). Novo harness `~/trading-journal-smoke/tradedetail-check.js` (jsdom + `main.js` + `styles.css` real) corre **31/31 PASS**. Norma registada: `UX-GUIDELINES.md` §5.2 (prioridade de leitura + clamp) e `UI-CATALOG.md` §6.3.1/§6.3.2 (breakpoints + hero como cartão). Gate: build 0 · smoke 142/0 · audit só dívida de base · `tradedetail-check` 31/31 · `fills-check` OK. Deploy vault-dev md5 ok (`main.js 0bfeb891`, `styles.css 39c4aa20`, `manifest.json 4395b22f`), `data.json` intacto. |
| 2026-09-21 | **Review engine v2 + Trade Detail v3.3 (BJ)**: o motor de review ganha **dois níveis** e o Trade Detail é reconstruído ao mockup `previews/trade-detail-v3.html`. **Modelo**: `Trade.psychologyAcknowledged` / `Trade.mistakesAcknowledged` (opcionais; `storage.ts` escreve `*_acknowledged: true` só quando true e lê-os de volta). **`lib/review.ts`**: required (decidem `complete`) — Screenshot · Strategy · Review · Rating; optional (nunca bloqueiam) — Psychology State · Execution Mistakes, cada um `done` com ≥1 tag **ou** acknowledgement. `complete = requiredDone === 4 && reviewed !== false` (fim do override absoluto de `reviewed:true`; `reviewed:false` continua a forçar incompleta); uma trade sem psicologia/mistakes e sem acknowledgement **continua completa** (o journal reporta, nunca impõe). `checks` com 6 entradas e flag `required`; `done`/`total` 0..6, `requiredDone`/`requiredTotal` 0..4, `label` `"Complete"`/`"N/4"`, `missing` só required; novo `optionalSummary(t)` (`"5 psychology · 3 mistakes"`, `"no mistakes"`, `"no psychology"`, `""`; singulariza `1 mistake`). Consumidores (`tradeDetailView` 4 dots + linha de summary + botões `.tj-td-ack`; `tradeLogView`; `dashboard`) falam todos por `reviewStatus(t).complete` / `reviewSummary`; ligar um chip de mistake desliga a acknowledgement. **Trade Detail v3.3**: hero numa linha (`.tj-td-hero-title-group` + `.tj-td-hero-metrics` com Net P&L · Points · Hold Time; fallback a 720px); grid `400px`/`340px`/`1fr` (1399/900); Execution & Risk com R-Multiple · Entry→Exit · Contracts · Stop/Risk · Session · Fees + `<details>` (Target · Planned R:R · Order Type · Max position · Fill count); Strategy+Rating em `.tj-td-two-col` com estrelas soltas e chips `.on`/`.is-mistake`; viewer `clamp(180px,28vh,280px)`, thumb activa `2px` verde. CSS morto removido (`.tj-td-tz`, `.tj-td-stars-inline`, `.tj-td-flip-row-rating`, `.tj-td-stat-value` do privacy; `.tj-td-hero-sym/-dir/-status`); **0 classes `.tj-td-*` sem consumidor**. Harness novo `~/trading-journal-smoke/review-check.js` (10 cenários / 32 asserções, trades em memória, nada escrito na vault). Gate: build 0 · smoke **142/0** · audit só dívida de base · `review-check` 10/10 · `tradedetail-check` 31/31 · deploy vault-dev md5 verificado (`main.js 92fb4d8668a5bfb5b581d9acef1f8519`, `styles.css 20832dc8afa734fe97926632d40ce1ca`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` intacto. |
| 2026-09-22 | **Print Annotator — seta com pontos livres (Catmull-Rom) + edição de pontos**: o modelo *curve type* (straight/curved/elbow) **sai** de `printAnnotator.ts` — tipo `ArrowType`, campo `arrowType` do `Shape`, `currentArrowType` e a secção *Arrow type* da sidebar (a sidebar de seta fica só em *Arrowheads*: ←, →, ↔). `arrowGeometry(pts)` passa a ser a única fonte de verdade da polilinha: suaviza os pontos com Catmull-Rom→Bézier (2 pontos = recta) e devolve path + pontos amostrados (32/segmento) + ângulos de tangente usados por desenho, hit-test e bbox; os ramos `line`/`arrow` do `drawShape` unificam-se (linha = mesma polilinha sem cabeças). **Edição de pontos** (arrow/line/pen): duplo-clique na forma ou tecla `E` (com seleção) entra; handles desenhados em canvas (pontos 8px branco com bordo de acento, midpoints tracejados) — arrastar move, clique num midpoint insere, duplo-clique num ponto intermédio apaga (extremos preservados); sai com clique fora, `Esc`, troca de tool, apagar, undo ou reset (`exitPointEdit`). Loader ignora `arrowType` legado; sem CSS novo. Gate: build 0 · smoke 142/0 · review-check OK · tradedetail-check OK · audit `no normative violations found` (2 lit. de 9px no `.tj-pq-*` → `var(--tj-fs-label)`) · deploy vault-dev md5 verificado (`main.js 4cd8d28b79e96a450534b2d7348ca9c4`, `styles.css f732f796f4564650335e5a0e072eff36`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` intacto. |
| 2026-09-22 | **Export Trade Card — linguagem v3.3 (poster partilhável)**: `showExportPanel()` + `buildExportCanvas()` em `tradeDetailView.ts` reconstruídos à linguagem do Trade Detail v3.3 mantendo a hierarquia de pôster. **FIX 1** — badge de estado passa de "Review Pending" (âmbar antigo) para `reviewStatus(t).complete` → **REVIEWED** (verde `#34d17a`) / **NEEDS REVIEW** (`--tj-tone-mid` `#d9a441`), pílula preenchida com a mesma tipografia da LONG, alinhada ao título. **FIX 2** — `Stop`, `Target`, `Risk $`, `Contracts` (4 campos) passam a **Stop / Risk · Target · Contracts** (3 por linha): valor `{stop} / ${risk}` com `risk = |entry − stop| × pointValue × qty` (`toFixed(0)`); sem stop/entry → `— / —`; **Hide P&L** mantém só o stop (`{stop} / —`). **FIX 3** — estrelas soltas ★ diretas no canvas (26px, gap 6, cheias `#f5b301`, vazias `rgba(255,255,255,0.15)`); `drawStarCanvas` removido (dead). **FIX 4** — hero: título + badge LONG/SHORT (tinta 12%) + badge de estratégia (pílula neutra: hairline + 4% branco, texto `--tj-fg-3`, só com `t.setup`) + estado ao fundo à direita; meta (data · entry · sessão) na 2ª linha a 16px. **FIX 5** — statline sem caixas: 4 colunas separadas por hairlines verticais `rgba(255,255,255,0.08)`, label 10px uppercase + número 32px bold (Net P&L com verde/vermelho por sinal, restantes `#dcddde`); coluna Risk $ inexistente (junta no Stop / Risk). **FIX 6** — toggle novo *Psychology & Mistakes* (default off) + secção no cartão entre rating e notas: labels 10px uppercase, chips pílula hairline 12px com 4px de gap (psychology com texto accent `--interactive-accent` resolvido do canvas, mistakes `--color-red-bright`), sub-secções vazias saltam; altura pré-medida (`chipRowsOf`) como as notas. **FIX 7** — watermark "Tradebook" removido. **FIX 8** — fundo fixo `#0d0d0d` mantido. Botões PNG/JPG/Copy, tamanho do canvas, screenshot e reset intactos. Gate: build 0 · smoke **142/0** · review-check OK · tradedetail-check OK · audit `no normative violations found` · deploy vault-dev md5 verificado (`main.js 9a8fa24ed9ff9db72a3374ed6657fe85`, `styles.css f732f796f4564650335e5a0e072eff36`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` intacto. |
| 2026-09-22 | **Export Trade Card — polish v3.3 (A: hero, B: estrelas 20px, C: hairlines + Session)**: tudo em `buildExportCanvas()` (`tradeDetailView.ts`). **A (hero)** — título passa a string única `"MES · Long"`/`"MES · Short"` (42px bold, `letterSpacing "-0.01em"` no canvas, `#f0f1f2`); badge LONG/SHORT e badge de estratégia logo a seguir (+16/+10), badge de estado **alinhado à direita** na mesma linha do título (bullet *Layout* do pedido, que é o explícito sobre placement); 2ª linha só com `formatDate(t.date, settings.dateFormat) + ", " + entryTime` a 16px `#8a9099` — **sessão sai do hero**. Header: 120→100. **B (estrelas)** — ★ desenhados como path de 5 pontas com **raio exterior 10 (20px de diámetro)**, gap 8px, cheias `#f5b301` / vazias `rgba(255,255,255,0.15)` (medida determinística em vez de glifo, que renderizava ~14px); label *EXECUTION RATING* 10px uppercase `--tj-fg-3`; bloco 70→54. **C (hairlines `rgba(255,255,255,0.06)`, inset PAD)** — par 1 hero↔screenshot e par 2 screenshot↔stats já existiam; **novo** par 3 statline↔fields (dentro do bloco stats, +32); par 4 fields↔rating = separador trailing do stats; par 5 rating↔psych e par 6 psych↔notes mantidos; separador de notes deixa de incluir `opts.stats` (stats já termina em hairline — fim da linha dupla). **Fields row** — 4ª coluna **Session** via `sessionLabel(t, zone)` (import novo de `../lib/sessions`, com `SESSION_UNKNOWN` → `—`); `SESSION_LABELS` saiu do import (ficava dead — o hero já não o lê); import novo `formatDate` de `../lib/dates`. Intocáveis: screenshot, toggles, botões, fundo `#0d0d0d`, os 4 KPIs, footer removido. Gate: build 0 · smoke **142/0** · review-check OK · tradedetail-check OK · audit `no normative violations found` · deploy vault-dev md5 verificado (`main.js 4e3d77c71b7d233c39b728c3086bc954`, `styles.css f732f796f4564650335e5a0e072eff36`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` intacto. |
| 2026-09-22 | **Export Trade Card — notas v3.3 no lugar de Thesis/Review/Improvements**: em `showExportPanel()` + `buildExportCanvas()` (`tradeDetailView.ts`). **Toggles** — saem *Thesis*, *Review* e *Improvements* (chaves `thesis`/`review`/`mistakes` do `opts` e as respetivas branches do canvas — greps `noteItems`/`opts.thesis|review|mistakes` a 0); o grupo *Review Notes* fica só com **Notes** ("What you wrote on the trade", default OFF). **Fallback da secção** — `notes → review → thesis` (primeiro não-vazio, `.trim()`); vazio → secção não desenha (sem heading órfão). Formato: label `NOTES` 10px uppercase `#8a9099` + corpo **14px `#dcddde`** quebrado pelo `wrapText` existente; `lineH` 24→20 (linha de 14px), bloco `24 + linhas×20 + 16` (altura pré-medida igual). **Mistakes com fallback legado** — `mistake_tags` (v3.3); se vazio e `t.mistake` não-vazio → esse string como **um** chip; psicologia continua só em `psychology_tags`. **Intocados**: screenshot, 4 KPIs, fields row (Stop/Risk · Target · Contracts · Session), estrelas, chips de psych, hero, hairlines (par 6 psych↔notes mantido via `showPsych || opts.rating`), botões, nota JPG/PNG, fundo `#0d0d0d`. Gate: build 0 · smoke **142/0** · review-check OK · tradedetail-check OK · audit `no normative violations found` · deploy vault-dev md5 verificado (`main.js 7c187afc78dc42ed25c2c06db3b4559f`, `styles.css f732f796f4564650335e5a0e072eff36`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`); `data.json` não copiado (mudou só por interação no Obsidian, `93e0c331…`). |
| 2026-09-22 | **Points = a maior distância real de saída à entrada (não a excursão teórica × contratos)** — regra nova em `src/lib/fills.ts`: **`isBreakEven(f, entry, pv)`** (exit a ≤0.5 pt da entrada **e** `\|pnl\| ≤ max(0.01, 0.5·pv·qty)` — não há flag BE no fill), **`pointsOf(dir, entry, exits, pv, explicit)`** (LONG: `max(saidas não-BE) − avgEntry`; SHORT: `avgEntry − min(…)`; sem saidas ou tudo BE → 0; sem fills guardados → a única saída nunca é BE) e **`tradePoints(t)`** (wrapper via `fillSet` + `futuresSpec`). **Escritores substituídos**: `csv.ts` `pairRoundTrips` — saía `points = gross / spec.pointValue` (embutia qty: o bug +16.25 = (3.00×5 + 0.25×5)×2/2) → `pointsOf(pos.dir, avgEntry, exitFills, pv, explícito⇔fills guardados)`; `addTradePanel` paint + doSave — `pts × qty` → `pts` (qty nunca multiplica); `copy.ts` `buildLeg` — `grossPnl`/`pnlPoints` da perna passam a usar **`tradePoints(base)`** (a decisão `hasPoints` continua a ler o valor guardado); `applyFillsToTrade` também regenera `pnlPoints`. **Leitura**: `storage.parseTradeFromMarkdown` continua a ler `pnl_points`; **recompute em memória no display** quando existem fills — `main.loadTrades` (2 ramos) e `TradeDetail.setTrade` — a nota só é reescrita quando o utilizador edita. **Execuções** (`renderExecutions`): labels `T…` → **`TP1/TP2/…` só sobre saídas não-BE** (`fillLabel` 4.º arg `pointValue`; BE → tag `BE`, classe `is-flat`, sem sufixo `· BE`), linha BE com **Points 0**, coluna Points da linha **Total = `trade.pnlPoints`** (antes era `—`); off-by-one corrigido (passava `set.fills.indexOf` global → `fillIndex`). Ledger (`tradeTable.drawFillRow` setup) usa a mesma label. **Intocados**: P&L $, R, fees, win/loss, $ das pernas de copy, hero/export/coluna Points (leem `t.pnlPoints`). Gate: build 0 · smoke **142/0** · review-check OK · tradedetail-check OK · **fills-check OK** (labels `entry \| TP1 \| TP2`, BE sem número, `tradePoints` 18/20/0, Total `+18.00`; harness actualizado) · audit `no normative violations found` · `ledger/tradelog/numbers-check` já falhavam **antes** desta tarefa (drift pré-existente) · deploy vault-dev md5 (`main.js 134dcdc74852b28d6ad3e2c8c8730482`, `styles.css f732f796f4564650335e5a0e072eff36`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` não tocado. |
| 2026-09-22 | **Trade Detail FIX 1–7 — Target em $, Execution & Risk sem colapso, Points sem ×qty, P&L por fill**: **FIX 1** `tradeDetailView` ganha `parsePriceOrDollars(raw, "stop"\|"target")` partilhado (número = preço; `$X` → `dist = $/pv/qty`, target no lado vencedor, stop no perdedor; input text; clique delegado no card para stop **e** target — cancelar não deixa span morto); display Target `{fmtPrice} / ${\|Δ\|×pv×qty}` (`$` a 2 casas; sem target `— / —`); grava só o preço (`saveFields({ target })`; I/O de `storage` verificado round-trip — `num()` escreve, `get()` des-aspóna). **FIX 2** o `<details class="tj-td-more">`/"More details" e **Planned R:R** saem; linhas todas abertas: Entry → Exit · R-Multiple · Contracts · Stop / Risk · **Target** · Session · Fees · Order Type · Max position · Fill count; CSS morto `.tj-td-more*` removido. **FIX 3** guards `(t.fills ?? []).length` saem de `main.loadTrades` (2 ramos) e `setTrade` → `pnlPoints = tradePoints(t)` **sempre** (em memória): o ×qty antigo (`addTradePanel` `pts*qty` / `csv.ts` `gross/pv`, removidos na §2.94) sobrevivia no display nas trades **scalar** — nota real `vault-dev/…/25-08-2026 MNQ LONG 0944.md:13` com `pnl_points: 57.5` = 11.5×5, sem bloco `fills`; a nota só é reescrita no edit (hero passa a `+11.50`). **FIX 4** `fills.ts` `inferred()`: a saída inferida passa a carregar `fees = commission+fees` e `pnl = dist×pv×qty − fees` (guard `exit>0`) — P&L por fill deixa de ser `—`, fees totais deixam de ser `$0.00`; Points por fill = distância própria à `avgEntry` sinalizada (BE 0, entrada `—`). **FIX 5** linha Total Price = `avgExit` (antes `avgEntry`; sem saídas → `—`). **FIX 6** Total Points = `t.pnlPoints` (já §2.94; com o FIX 3 dá `+11.50`). **FIX 7** `fillLabel` perde `exits≤1 → "exit"` → saída não-BE única é **TP1** (ledger e trade page concordam). Gate: build 0 · smoke **142/0** · review-check OK · tradedetail-check OK (`[fields]` reescrito: ordem exacta das 10 linhas, Target `21,040 / $320.00`, stop `$200`→`20,975`) · fills-check OK · audit `no normative violations found` · `ledger/tradelog/numbers-check` drift pré-existente · deploy vault-dev md5 (`main.js fc3679f351ee7c734b2bae0562742157`, `styles.css 701fde2ca7a3ce4ee4c42f626f82fe73`, `manifest.json 4395b22f3733eeb1c69bcc3f0a0aeaec`), `data.json` não tocado (`89baa19a…`). |
| 2026-09-22 | **BUG 2 revert — `pnl` volta a ser GROSS, o net é derivado (`netPnl`), `grossPnl` sai do schema**: o `pnl` da trade volta a ser o **resultado bruto** (points × pointValue × qty); `commission`/`fees` ficam em campos próprios e o **net é sempre derivado** — `netPnl(t) = round2(pnl − commission − fees)`, novo em `lib/fees.ts`, nunca gravado. `Trade.grossPnl` e o YAML `gross_pnl` **saem** de `types.ts`/`storage.ts` (notas antigas com a chave continuam a carregar: chave desconhecida ignorada, e o `NaN` latente de `parseFloat` de uma chave ausente desaparece). **Escritores**: `addTradePanel` paint/doSave e `csv.ts` `pairRoundTrips` gravam `pnl = gross`; a cola de custos órfãos (`csv.ts` `:434`) deixa de reescrever o pnl (só soma a commission/fees); `copy.ts` `buildLeg` escala o pnl do base (já gross); `fills.ts` `applyFillsToTrade` soma os fills (gross) para o pnl. **Migração para NET (no mesmo build — regra crítica)**: `accountMetrics` passa a **dois acumuladores por dia** — net para balance/dd/consistency/target/today/dailyLoss, gross para best/worst day + dayWinRate/winDays; `expectancy` passa a gross; `accountDashboard` runningBalance/equity, `accountsListView` strip/curva/cells, `dashboard` `drawEquityChart`, `ui.ts` `cumulativeEquitySeries`, `main.ts` breach-check + target-date e `importUi` `balanceCheck` passam a `netPnl`. **Labels** (o rótulo segue o valor): hero do Trade Detail, `tradeModal`, `dayLogModal` e `m.netpnl` passam a **"P&L"** (gross); onde o valor é net fica "Net P&L". **Coluna nova** `net` em `TRADE_COLUMNS` (render `netPnl`), **oculta por default** — só entra pelo column picker. Gate: build 0 · smoke **142/0** · audit `no normative violations found`. |
